// Вспомогательные функции для шаблонов
window.escHtml = function(s) {
  return String(s ?? '').replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
};
window.val = function(data, key, ph = '_______________') {
  const v = (data?.[key] ?? '').toString().trim();
  return v ? escHtml(v) : ph;
};
window.money = function(data, key, ph = '_______') {
  const v = (data?.[key] ?? '').toString().trim();
  return v ? escHtml(v) : ph;
};
window.dateRu = function(v) {
  const s = (v ?? '').toString().trim();
  if (/^\d{4}-\d{2}-\d{2}$/.test(s)) {
    const [y, m, d] = s.split('-');
    return `${d}.${m}.${y}`;
  }
  return s || '_______________';
};
window.rightBlock = function(lines) {
  return (lines || []).map((t) => `<div class="party-line">${t}</div>`).join('');
};
window.p = function(t) {
  return `<div class="text-block">${t}</div>`;
};
window.reqBlock = function(items) {
  return (items || []).map((t, i) => `<div class="request-item">${i + 1}. ${t}</div>`).join('');
};
window.signBlock = function(leftText, rightText) {
  return `
    <div style="display:flex;justify-content:space-between;width:100%;margin-top:2em;">
      <table style="border-collapse:collapse;width:auto;"><tr><td style="border:none;padding:0;">${leftText}</td></tr></table>
      <table style="border-collapse:collapse;width:auto;"><tr><td style="border:none;padding:0;">${rightText}</td></tr></table>
    </div>
  `;
};

window.externalTemplates = {
  // ==================== ИСКОВЫЕ ЗАЯВЛЕНИЯ (claim) ====================

  'claim-damage-crime': {
    category: 'claim',
    title: 'О возмещении вреда, причиненного преступлением',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text', placeholder: 'Курганский городской суд' },
      { name: 'court_address', label: 'Адрес суда', type: 'text', placeholder: '640000, г. Курган, ул. Судейская, д. 101' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text', placeholder: 'г. Курган, ул. Советская, д. 110, кв. 5' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text', placeholder: '+7 (111) 111-11-11' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text', placeholder: 'Сидоров Сергей Петрович' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text', placeholder: 'г. Курган, ул. Мира, д. 111, кв. 4' },
      { name: 'defendant_phone', label: 'Телефон ответчика', type: 'text', placeholder: '+7 (111) 111-22-22' },
      { name: 'third_party', label: 'Третье лицо (если есть)', type: 'text', placeholder: 'ООО «Страховая компания»' },
      { name: 'third_party_address', label: 'Адрес третьего лица', type: 'text', placeholder: 'г. Курган, ул. Страховщиков, д. 102' },
      { name: 'crime_date', label: 'Дата преступления', type: 'text', placeholder: 'октябрь 2023' },
      { name: 'property_damage', label: 'Материальный ущерб (руб.)', type: 'number', placeholder: '111000' },
      { name: 'moral_damage', label: 'Моральный вред (руб.)', type: 'number', placeholder: '50000' },
      { name: 'conviction_date', label: 'Дата приговора суда', type: 'text', placeholder: '25 декабря 2023 года' },
      { name: 'crime_desc', label: 'Описание преступления (кратко)', type: 'textarea', placeholder: 'кража из квартиры, повреждение имущества' }
    ],
    template: (data) => `
      <div class="court-line">В ${data.court || '_______________'}</div>
      <div class="court-address">${data.court_address || '_______________'}</div>
      <div class="party-line">Истец: ${data.plaintiff || '_______________'}</div>
      <div class="party-line">Адрес: ${data.plaintiff_address || '_______________'}</div>
      <div class="party-line">Телефон: ${data.plaintiff_phone || '_______________'}</div>
      <div class="party-line">Ответчик: ${data.defendant || '_______________'}</div>
      <div class="party-line">Адрес: ${data.defendant_address || '_______________'}</div>
      <div class="party-line">Телефон: ${data.defendant_phone || '_______________'}</div>
      <div class="party-line">Третье лицо: ${data.third_party || '_______________'}</div>
      <div class="party-line">Адрес: ${data.third_party_address || '_______________'}</div>
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о возмещении вреда, причиненного преступлением</div>
      <div class="text-block">С ${data.crime_date || '_______________'} я, ${data.plaintiff || '_______________'}, являясь собственником квартиры, временно отсутствовал. По возвращении обнаружил, что квартира была ограблена, имущество повреждено. Приговором ${data.court || '_______________'} от ${data.conviction_date || '_______________'} ответчик признан виновным в совершении преступления: ${data.crime_desc || '_______________'}. Действия ответчика нанесли мне материальный ущерб на сумму ${data.property_damage || '_______'} руб., а также причинили физические и нравственные страдания.</div>
      <div class="text-block">Ответчик до настоящего времени не возместил ущерб добровольно.</div>
      <div class="legal-ref">На основании ст. 1064, 151, 1099 ГК РФ, ст. 44 УПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      <div class="request-item">1. Взыскать с ответчика в мою пользу материальный ущерб в размере ${data.property_damage || '_______'} руб.</div>
      <div class="request-item">2. Взыскать компенсацию морального вреда в размере ${data.moral_damage || '_______'} руб.</div>
      <div class="request-item">3. Взыскать судебные расходы.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${data.plaintiff || '_______________'} _________________`)}
    `
  },

  'claim-damage-health': {
    category: 'claim',
    title: 'О возмещении вреда здоровью, причиненного ДТП',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text', placeholder: 'Курганский городской суд' },
      { name: 'court_address', label: 'Адрес суда', type: 'text', placeholder: '111111, г. Курган, ул. Судейская, д. 10' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text', placeholder: 'Иванов Александр Петрович' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text', placeholder: '111111, г. Курган, ул. Лесная, д. 5, кв. 10' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text', placeholder: '+7 (111) 111-11-11' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text', placeholder: 'Петров Сергей Викторович' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text', placeholder: '111111, г. Курган, ул. Заречная, д. 7, кв. 4' },
      { name: 'defendant_phone', label: 'Телефон ответчика', type: 'text', placeholder: '+7 (111) 222-22-22' },
      { name: 'accident_date', label: 'Дата ДТП', type: 'date', placeholder: '2024-05-15' },
      { name: 'treatment_costs', label: 'Расходы на лечение (руб.)', type: 'number', placeholder: '111000' },
      { name: 'rehab_costs', label: 'Расходы на реабилитацию (руб.)', type: 'number', placeholder: '22000' },
      { name: 'moral_damage', label: 'Компенсация морального вреда (руб.)', type: 'number', placeholder: '150000' },
      { name: 'lost_earnings', label: 'Утраченный заработок (руб.)', type: 'number', placeholder: '50000' }
    ],
    template: (data) => `
      <div class="court-line">В ${data.court || '_______________'}</div>
      <div class="court-address">${data.court_address || '_______________'}</div>
      <div class="party-line">Истец: ${data.plaintiff || '_______________'}</div>
      <div class="party-line">Адрес: ${data.plaintiff_address || '_______________'}</div>
      <div class="party-line">Телефон: ${data.plaintiff_phone || '_______________'}</div>
      <div class="party-line">Ответчик: ${data.defendant || '_______________'}</div>
      <div class="party-line">Адрес: ${data.defendant_address || '_______________'}</div>
      <div class="party-line">Телефон: ${data.defendant_phone || '_______________'}</div>
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о возмещении вреда здоровью, причиненного дорожно-транспортным происшествием</div>
      <div class="text-block">${dateRu(data.accident_date)} на перекрестке улиц Советская и Ленина в г. Курган произошло ДТП, в результате которого я получил повреждения здоровья. Виновником признан ответчик. В результате аварии я получил травмы, расходы на лечение составили ${money(data, 'treatment_costs')} руб., расходы на реабилитацию и лекарства — ${money(data, 'rehab_costs')} руб., утраченный заработок — ${money(data, 'lost_earnings')} руб. Действия ответчика причинили мне физические и моральные страдания.</div>
      <div class="legal-ref">На основании ст. 1064, 1079, 1085, 1086, 1099-1101 ГК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      <div class="request-item">1. Взыскать с ответчика расходы на лечение в размере ${money(data, 'treatment_costs')} руб.</div>
      <div class="request-item">2. Взыскать расходы на реабилитацию и лекарства в размере ${money(data, 'rehab_costs')} руб.</div>
      <div class="request-item">3. Взыскать утраченный заработок в размере ${money(data, 'lost_earnings')} руб.</div>
      <div class="request-item">4. Взыскать компенсацию морального вреда в размере ${money(data, 'moral_damage')} руб.</div>
      <div class="request-item">5. Взыскать судебные расходы.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${data.plaintiff || '_______________'} _________________`)}
    `
  },

  'claim-debt-loan': {
    category: 'claim',
    title: 'О взыскании долга по договору займа',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text', placeholder: '_______ районный суд г. _______' },
      { name: 'court_address', label: 'Адрес суда', type: 'text', placeholder: 'адрес суда' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика', type: 'text' },
      { name: 'loan_date', label: 'Дата займа', type: 'date' },
      { name: 'loan_amount', label: 'Сумма займа (руб.)', type: 'number' },
      { name: 'due_date', label: 'Срок возврата (дата)', type: 'date' },
      { name: 'interest_rate', label: 'Процентная ставка (% годовых)', type: 'number', placeholder: 'если предусмотрена' },
      { name: 'pretension_date', label: 'Дата претензии (если направлялась)', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`,
        `Тел.: ${val(data, 'defendant_phone')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании долга по договору займа</div>
      ${p(`${dateRu(data.loan_date)} между мной и ответчиком был заключён договор займа, по которому ответчик получил денежные средства в сумме ${money(data, 'loan_amount')} руб. со сроком возврата до ${dateRu(data.due_date)}. Факт передачи денег подтверждается распиской (или иными документами).`)}
      ${p(`До настоящего времени ответчик сумму займа не возвратил, обязательство не исполнил.`)}
      ${data.pretension_date ? p(`Претензия направлялась ${dateRu(data.pretension_date)} (прилагается подтверждение). Требования добровольно не удовлетворены.`) : p(`Досудебное урегулирование осуществлялось, однако ответчик уклоняется от исполнения обязательств.`)}
      <div class="legal-ref">На основании ст. 309, 310, 807–811 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ${val(data, 'defendant')} в пользу ${val(data, 'plaintiff')} сумму долга по договору займа в размере ${money(data, 'loan_amount')} руб.`,
        `Взыскать проценты за пользование займом (если предусмотрены договором) в размере ________ руб.`,
        `Взыскать судебные расходы, включая госпошлину.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-debt-receipt': {
    category: 'claim',
    title: 'О взыскании долга по расписке',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'receipt_date', label: 'Дата расписки', type: 'date' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'return_date', label: 'Срок возврата', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}, ${val(data, 'plaintiff_address')}`,
        `Ответчик: ${val(data, 'defendant')}, ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании долга по расписке</div>
      ${p(`${dateRu(data.receipt_date)} ответчик выдал(а) расписку о получении от меня денежных средств в сумме ${money(data, 'amount')} руб. со сроком возврата до ${dateRu(data.return_date)}.`)}
      ${p(`В установленный срок денежные средства не возвращены.`)}
      <div class="legal-ref">На основании ст. 309, 310, 808, 810 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика сумму долга ${money(data, 'amount')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-consumer-refund': {
    category: 'claim',
    title: 'О защите прав потребителя (возврат денег, неустойка, штраф)',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'defendant_org', label: 'Ответчик (организация)', type: 'text', placeholder: 'ООО "..."' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'product_service', label: 'Товар/услуга', type: 'text' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'purchase_date', label: 'Дата покупки/оплаты', type: 'date' },
      { name: 'claim_date', label: 'Дата претензии', type: 'date' },
      { name: 'problem', label: 'Суть недостатка/нарушения', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Ответчик: ${val(data, 'defendant_org')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о защите прав потребителя</div>
      ${p(`${dateRu(data.purchase_date)} мной был(а) приобретён(а)/оплачен(а) ${val(data, 'product_service')} стоимостью ${money(data, 'price')} руб.`)}
      ${p(`Нарушение/недостаток: ${val(data, 'problem')}.`)}
      ${p(`Претензия направлена ${dateRu(data.claim_date)}. Требования добровольно не удовлетворены.`)}
      <div class="legal-ref">На основании Закона РФ «О защите прав потребителей» (ст. 13, 15, 18/29, 22/31), ст. 309 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика стоимость ${val(data, 'product_service')} в размере ${money(data, 'price')} руб.`,
        `Взыскать неустойку за нарушение сроков удовлетворения требований потребителя (расчёт прилагается).`,
        `Взыскать компенсацию морального вреда (размер по усмотрению суда).`,
        `Взыскать штраф 50% от присуждённой суммы за неудовлетворение требований потребителя в добровольном порядке.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-salary-arrears': {
    category: 'claim',
    title: 'О взыскании задолженности по заработной плате и компенсации за задержку',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'employer', label: 'Ответчик (работодатель)', type: 'text' },
      { name: 'employer_address', label: 'Адрес работодателя', type: 'text' },
      { name: 'period', label: 'Период задолженности', type: 'text', placeholder: 'январь–март 2026' },
      { name: 'amount', label: 'Сумма задолженности (руб.)', type: 'number' },
      { name: 'compensation', label: 'Денежная компенсация по ст. 236 ТК РФ (руб.)', type: 'number', placeholder: 'расчёт прилагается' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Истец: ${val(data, 'plaintiff')}, ${val(data, 'plaintiff_address')}`,
        `Ответчик: ${val(data, 'employer')}, ${val(data, 'employer_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании задолженности по заработной плате</div>
      ${p(`Я состоял(а) в трудовых отношениях с ${val(data, 'employer')}. За период ${val(data, 'period')} заработная плата выплачена не полностью/не выплачена. Размер задолженности составляет ${money(data, 'amount')} руб. (расчёт прилагается).`)}
      ${p(`В соответствии со ст. 236 ТК РФ за задержку выплаты подлежит взысканию денежная компенсация в размере ${money(data, 'compensation')} руб.`)}
      <div class="legal-ref">На основании ст. 22, 136, 140, 236 ТК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика задолженность по заработной плате ${money(data, 'amount')} руб.`,
        `Взыскать денежную компенсацию за задержку выплат по ст. 236 ТК РФ в размере ${money(data, 'compensation')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-reinstatement': {
    category: 'claim',
    title: 'О восстановлении на работе и оплате времени вынужденного прогула',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'employer', label: 'Ответчик (работодатель)', type: 'text' },
      { name: 'dismissal_date', label: 'Дата увольнения', type: 'date' },
      { name: 'order_number', label: 'Приказ (номер)', type: 'text', placeholder: '№ ___' },
      { name: 'reason', label: 'Почему увольнение незаконно (кратко)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Ответчик: ${val(data, 'employer')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о восстановлении на работе</div>
      ${p(`${dateRu(data.dismissal_date)} я был(а) уволен(а) по приказу ${val(data, 'order_number')}. Считаю увольнение незаконным по следующим основаниям: ${val(data, 'reason')}.`)}
      <div class="legal-ref">На основании ст. 81, 84.1, 394 ТК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Восстановить меня на прежней работе у ${val(data, 'employer')}.`,
        `Взыскать средний заработок за время вынужденного прогула (расчёт прилагается).`,
        `Обязать ответчика внести исправления в кадровые документы (при необходимости).`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-eviction': {
    category: 'claim',
    title: 'О выселении и снятии с регистрационного учёта (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'address', label: 'Адрес жилого помещения', type: 'text' },
      { name: 'basis', label: 'Основание (собственность/прекращение семейных отношений и т.п.)', type: 'textarea' },
      { name: 'absence_start', label: 'С какого времени ответчик не проживает', type: 'text', placeholder: 'с _____ года' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Истец: ${val(data, 'plaintiff')}`, `Ответчик: ${val(data, 'defendant')}`])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о выселении и снятии с регистрационного учёта</div>
      ${p(`Жилое помещение по адресу: ${val(data, 'address')}. Основания требований: ${val(data, 'basis')}. Ответчик добровольно выехал из жилого помещения ${val(data, 'absence_start')}, вывез свои вещи, не оплачивает коммунальные услуги, препятствий в пользовании жильём не имел. Право пользования жилым помещением ответчик утратил, добровольно сниматься с регистрационного учёта отказывается.`)}
      <div class="legal-ref">На основании ст. 209, 304 ГК РФ, ст. 31, 35 ЖК РФ (по ситуации), ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Признать ${val(data, 'defendant')} утратившим(ей) право пользования жилым помещением по адресу: ${val(data, 'address')}.`,
        `Обязать уполномоченный орган снять ответчика с регистрационного учёта.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-unjust-enrichment': {
    category: 'claim',
    title: 'О взыскании неосновательного обогащения',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'transfer_date', label: 'Дата перечисления/передачи', type: 'date' },
      { name: 'circumstances', label: 'Обстоятельства (ошибка перевода и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Истец: ${val(data, 'plaintiff')}`, `Ответчик: ${val(data, 'defendant')}`])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании неосновательного обогащения</div>
      ${p(`${dateRu(data.transfer_date)} ответчик получил(а) от меня денежные средства в размере ${money(data, 'amount')} руб. при следующих обстоятельствах: ${val(data, 'circumstances')}. Правовых оснований для удержания денежных средств не имеется. Направленная претензия оставлена без удовлетворения.`)}
      <div class="legal-ref">На основании ст. 1102–1109 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика неосновательное обогащение в размере ${money(data, 'amount')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-penalty-contract': {
    category: 'claim',
    title: 'О взыскании неустойки по договору (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'contract_date', label: 'Дата договора', type: 'date' },
      { name: 'contract_subject', label: 'Предмет договора', type: 'text', placeholder: 'подряд/поставка/услуги' },
      { name: 'penalty_amount', label: 'Неустойка (руб.)', type: 'number' },
      { name: 'breach', label: 'Нарушение (кратко)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Истец: ${val(data, 'plaintiff')}`, `Ответчик: ${val(data, 'defendant')}`])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании неустойки</div>
      ${p(`${dateRu(data.contract_date)} стороны заключили договор (${val(data, 'contract_subject')}). Ответчиком допущено нарушение: ${val(data, 'breach')}. В соответствии с условиями договора подлежит уплате неустойка ${money(data, 'penalty_amount')} руб. (расчёт прилагается).`)}
      <div class="legal-ref">На основании ст. 309, 310, 330 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика неустойку в размере ${money(data, 'penalty_amount')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-alimony': {
    category: 'claim',
    title: 'О взыскании алиментов на несовершеннолетнего ребёнка (ГПК)',
    fields: [
      { name: 'court', label: 'Суд (наименование)', type: 'text', placeholder: '_______ районный суд г. _______' },
      { name: 'court_address', label: 'Адрес суда', type: 'text', placeholder: 'адрес суда' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text', placeholder: 'Иванова Мария Петровна' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text', placeholder: 'Иванов Сергей Иванович, 15.05.2015' },
      { name: 'share', label: 'Размер алиментов (доля/твёрдая сумма)', type: 'text', placeholder: '1/4 дохода / 10 000 руб. ежемесячно' },
      { name: 'marriage_status', label: 'Брак (зарегистрирован/расторгнут/не заключён)', type: 'text', placeholder: 'расторгнут ___.__.____' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании алиментов на несовершеннолетнего ребёнка</div>
      ${p(`У меня и ответчика имеется несовершеннолетний ребёнок: ${val(data, 'child')}. Брак ${val(data, 'marriage_status')}. Ответчик добровольно участия в содержании ребёнка не принимает, материальной помощи не оказывает. Соглашение об уплате алиментов между нами не заключалось.`)}
      ${p(`На основании ст. 80–83 СК РФ прошу взыскать алименты в размере: ${val(data, 'share')}.`)}
      <div class="legal-ref">На основании ст. 80–83, 107 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ${val(data, 'defendant')} алименты на содержание ребёнка ${val(data, 'child')} в размере ${val(data, 'share')} ежемесячно, начиная с даты подачи заявления до совершеннолетия ребёнка.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-divorce': {
    category: 'claim',
    title: 'О расторжении брака (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'marriage_date', label: 'Дата заключения брака', type: 'date' },
      { name: 'marriage_place', label: 'Орган ЗАГС (где зарегистрирован брак)', type: 'text' },
      { name: 'children', label: 'Дети (если есть)', type: 'text', placeholder: 'нет / ФИО, даты рождения' },
      { name: 'reason', label: 'Причины расторжения (кратко)', type: 'textarea', placeholder: 'Совместная жизнь не сложилась...' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о расторжении брака</div>
      ${p(`Брак между мной и ответчиком зарегистрирован ${dateRu(data.marriage_date)} в ${val(data, 'marriage_place')}.`)}
      ${p(`Дети: ${val(data, 'children')}.`)}
      ${p(`Причины: ${val(data, 'reason')}. Совместная жизнь и сохранение семьи невозможны, примирение не достигнуто. Спор о детях и разделе имущества отсутствует (либо урегулирован).`)}
      <div class="legal-ref">На основании ст. 21–23 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Расторгнуть брак между ${val(data, 'plaintiff')} и ${val(data, 'defendant')}, зарегистрированный ${dateRu(data.marriage_date)} в ${val(data, 'marriage_place')}.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-division-spouses': {
    category: 'claim',
    title: 'О разделе совместно нажитого имущества супругов (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'property', label: 'Имущество (перечень)', type: 'textarea' },
      { name: 'price', label: 'Цена иска (руб.)', type: 'number' },
      { name: 'marriage_end_date', label: 'Дата прекращения брачных отношений', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`,
        `Цена иска: ${money(data, 'price')} руб.`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о разделе совместно нажитого имущества супругов</div>
      ${p(`Брак между сторонами прекращён (расторгнут либо фактически прекращён с ${dateRu(data.marriage_end_date)}). Совместное хозяйство не ведётся. Соглашение о добровольном разделе имущества не достигнуто.`)}
      ${p(`В период брака сторонами совместно нажито следующее имущество: ${val(data, 'property')}.`)}
      ${p(`Доли супругов в совместном имуществе признаются равными. Прошу произвести раздел следующим образом: ... (подробно в приложении).`)}
      <div class="legal-ref">На основании ст. 34, 38–39 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Произвести раздел совместно нажитого имущества, выделив мне в собственность ..., ответчику — ...`,
        `Взыскать с ответчика денежную компенсацию (если стоимость переданного имущества превышает долю).`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-child-residence': {
    category: 'claim',
    title: 'Об определении места жительства ребёнка (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text' },
      { name: 'circ', label: 'Обстоятельства (почему с вами)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>об определении места жительства ребёнка</div>
      ${p(`Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Имеется спор о месте жительства ребёнка. Обстоятельства: ${val(data, 'circ')}.`)}
      ${p(`Проживание ребёнка со мной будет наилучшим образом соответствовать его интересам (учитывая возраст, привязанность, материальные и жилищные условия, моральный климат).`)}
      <div class="legal-ref">На основании ст. 65, 66 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Определить место жительства ребёнка ${val(data, 'child')} с ${val(data, 'plaintiff')}.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-child-communication': {
    category: 'claim',
    title: 'Об определении порядка общения с ребёнком (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text' },
      { name: 'schedule', label: 'Желаемый график общения', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>об определении порядка общения с ребёнком</div>
      ${p(`Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Ответчик препятствует моему общению с ребёнком, что нарушает мои родительские права. Прошу определить порядок общения: ${val(data, 'schedule')}.`)}
      <div class="legal-ref">На основании ст. 66 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Определить порядок общения ${val(data, 'plaintiff')} с ребёнком ${val(data, 'child')} согласно графику: ${val(data, 'schedule')}.`,
        `Обязать ответчика не чинить препятствий в общении.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-paternity': {
    category: 'claim',
    title: 'Об установлении отцовства и взыскании алиментов (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text' },
      { name: 'facts', label: 'Факты (совместное проживание, признание и т.п.)', type: 'textarea' },
      { name: 'share', label: 'Размер алиментов', type: 'text', placeholder: '1/4 дохода / ___ руб.' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>об установлении отцовства и взыскании алиментов</div>
      ${p(`Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Обстоятельства: ${val(data, 'facts')}. Отцовство ответчика в отношении ребёнка не зарегистрировано, однако он фактически является отцом. Ответчик добровольно не подаёт заявление об установлении отцовства и не оказывает материальной помощи на содержание ребёнка.`)}
      <div class="legal-ref">На основании ст. 49, 80–83 СК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Установить, что ответчик ${val(data, 'defendant')} является отцом ребёнка ${val(data, 'child')}.`,
        `Взыскать с ответчика алименты на содержание ребёнка в размере ${val(data, 'share')} ежемесячно.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-inheritance-restore-term': {
    category: 'claim',
    title: 'О восстановлении срока принятия наследства (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО/нотариус)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'decedent', label: 'Наследодатель (ФИО)', type: 'text' },
      { name: 'death_date', label: 'Дата смерти', type: 'date' },
      { name: 'reasons', label: 'Причины пропуска срока', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Заинтересованное лицо: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о восстановлении срока принятия наследства</div>
      ${p(`Наследодатель: ${val(data, 'decedent')}, дата смерти: ${dateRu(data.death_date)}.`)}
      ${p(`Я являюсь наследником по закону (завещанию). Шестимесячный срок для принятия наследства пропущен по уважительным причинам: ${val(data, 'reasons')}.`)}
      <div class="legal-ref">На основании ст. 1155 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Восстановить мне срок для принятия наследства после смерти ${val(data, 'decedent')}.`,
        `Признать меня принявшим(ей) наследство.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-establish-kinship-fact': {
    category: 'claim',
    title: 'Об установлении факта родственных отношений (особое производство, ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'interested', label: 'Заинтересованные лица (при наличии)', type: 'text' },
      { name: 'fact', label: 'Какой факт установить', type: 'textarea', placeholder: 'что Иванов И.И. является отцом...' },
      { name: 'purpose', label: 'Для чего нужно (цель)', type: 'textarea', placeholder: 'для оформления наследства...' },
      { name: 'evidence', label: 'Доказательства родства (документы, письма, показания)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Заявитель: ${val(data, 'applicant')}`,
        `Заинтересованные лица: ${val(data, 'interested')}`
      ])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>об установлении факта родственных отношений</div>
      ${p(`Прошу установить факт: ${val(data, 'fact')}.`)}
      ${p(`Цель установления факта: ${val(data, 'purpose')}.`)}
      ${p(`Доказательства: ${val(data, 'evidence')}. Восстановить документы во внесудебном порядке невозможно.`)}
      <div class="legal-ref">На основании гл. 28 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Установить указанный юридический факт родственных отношений.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },

  'claim-establish-document-ownership-fact': {
    category: 'claim',
    title: 'Об установлении факта принадлежности правоустанавливающего документа (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'doc', label: 'Документ (какой, реквизиты)', type: 'textarea' },
      { name: 'issue', label: 'В чём ошибка/несоответствие', type: 'textarea' },
      { name: 'purpose', label: 'Цель', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Заявитель: ${val(data, 'applicant')}`
      ])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>об установлении факта принадлежности правоустанавливающего документа</div>
      ${p(`Документ: ${val(data, 'doc')}.`)}
      ${p(`Несоответствие: ${val(data, 'issue')}.`)}
      ${p(`Цель установления факта: ${val(data, 'purpose')}.`)}
      <div class="legal-ref">На основании ст. 264–267 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Установить факт принадлежности указанного правоустанавливающего документа заявителю.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },

  'claim-flood-damages': {
    category: 'claim',
    title: 'О возмещении ущерба от залива квартиры (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО/УК)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'date', label: 'Дата залива', type: 'date' },
      { name: 'damage', label: 'Сумма ущерба (руб.)', type: 'number' },
      { name: 'evidence', label: 'Доказательства (акт, фото, смета)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о возмещении ущерба, причинённого заливом квартиры</div>
      ${p(`${dateRu(data.date)} произошёл залив принадлежащей мне квартиры по вине ответчика. Согласно акту о затоплении (иным доказательствам: ${val(data, 'evidence')}) причинён ущерб на сумму ${money(data, 'damage')} руб. (смета, заключение оценщика прилагаются).`)}
      ${p(`Добровольно ущерб не возмещён.`)}
      <div class="legal-ref">На основании ст. 15, 1064 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ${val(data, 'defendant')} ущерб в размере ${money(data, 'damage')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-defamation': {
    category: 'claim',
    title: 'О защите чести, достоинства и деловой репутации (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО/организация)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'info', label: 'Какие сведения и где распространены', type: 'textarea' },
      { name: 'proof', label: 'Доказательства распространения', type: 'textarea' },
      { name: 'moral', label: 'Компенсация морального вреда (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о защите чести, достоинства и деловой репутации</div>
      ${p(`Ответчик распространил в (указать способ) следующие сведения, не соответствующие действительности и порочащие меня: ${val(data, 'info')}.`)}
      ${p(`Факт распространения подтверждается: ${val(data, 'proof')}.`)}
      <div class="legal-ref">На основании ст. 152, 151 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Признать распространённые сведения не соответствующими действительности, порочащими честь, достоинство и деловую репутацию истца.`,
        `Обязать ответчика опровергнуть указанные сведения тем же способом.`,
        `Взыскать компенсацию морального вреда в размере ${money(data, 'moral')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-395-interest': {
    category: 'claim',
    title: 'О взыскании процентов по ст. 395 ГК РФ (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'principal', label: 'Основной долг (руб.)', type: 'number' },
      { name: 'period', label: 'Период просрочки (текстом)', type: 'text', placeholder: 'с __.__.____ по __.__.____' },
      { name: 'interest', label: 'Проценты (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о взыскании процентов за пользование чужими денежными средствами (ст. 395 ГК РФ)</div>
      ${p(`Ответчик неправомерно удерживает (уклоняется от возврата) денежные средства в размере ${money(data, 'principal')} руб. Период просрочки: ${val(data, 'period')}.`)}
      ${p(`Размер процентов по ст. 395 ГК РФ за указанный период составляет ${money(data, 'interest')} руб. (расчёт прилагается).`)}
      <div class="legal-ref">На основании ст. 309, 310, 395 ГК РФ, ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика проценты за пользование чужими денежными средствами в размере ${money(data, 'interest')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-utility-recalc': {
    category: 'claim',
    title: 'О перерасчёте платы за ЖКУ/коммунальные услуги (типовой, ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'court_address', label: 'Адрес суда', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'plaintiff_phone', label: 'Телефон истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (УК/РСО)', type: 'text' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'defendant_phone', label: 'Телефон ответчика (если известен)', type: 'text' },
      { name: 'period', label: 'Период', type: 'text' },
      { name: 'facts', label: 'Нарушение (качество/перерывы/начисления)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `${val(data, 'court_address')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Тел.: ${val(data, 'plaintiff_phone')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Адрес: ${val(data, 'defendant_address')}`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ<br>о перерасчёте платы за жилищно-коммунальные услуги</div>
      ${p(`Ответчик (${val(data, 'defendant')}) неверно производил начисления (оказывал услуги ненадлежащего качества/с перерывами) за период ${val(data, 'period')}. Обстоятельства: ${val(data, 'facts')}.`)}
      ${p(`Претензия оставлена без удовлетворения.`)}
      <div class="legal-ref">На основании ЖК РФ, ст. 29 Закона РФ «О защите прав потребителей», ст. 131–132 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Обязать ответчика произвести перерасчёт платы за указанный период в соответствии с законодательством.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-arb-debt-supply': {
    category: 'claim',
    title: 'О взыскании задолженности по договору поставки (АПК, ИП/ООО)',
    fields: [
      { name: 'court', label: 'Арбитражный суд', type: 'text', placeholder: 'Арбитражный суд ______ области/края/г. Москвы' },
      { name: 'plaintiff', label: 'Истец (ИП/ООО/ФИО)', type: 'text', placeholder: 'ИП Иванов И.И.' },
      { name: 'plaintiff_inn', label: 'ИНН истца (если есть)', type: 'text', placeholder: '__________' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ООО/ИП)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'defendant_inn', label: 'ИНН ответчика (если известен)', type: 'text', placeholder: '__________' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'price', label: 'Цена иска (руб.)', type: 'number', placeholder: '100000' },
      { name: 'contract', label: 'Договор поставки (№, дата)', type: 'text' },
      { name: 'debt', label: 'Задолженность (руб.)', type: 'number' },
      { name: 'pretrial', label: 'Претензия (дата/исх.)', type: 'text', placeholder: 'направлена __.__.____, исх. №__' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Истец: ${val(data, 'plaintiff')} (ИНН ${val(data, 'plaintiff_inn')})`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Ответчик: ${val(data, 'defendant')} (ИНН ${val(data, 'defendant_inn')})`,
        `Адрес: ${val(data, 'defendant_address')}`,
        `Цена иска: ${money(data, 'price')} руб.`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ (АПК РФ)<br>о взыскании задолженности по договору поставки</div>
      ${p(`Между истцом и ответчиком заключён договор поставки ${val(data, 'contract')}. Истец надлежащим образом поставил товар, однако ответчик обязанность по оплате не исполнил.`)}
      ${p(`Задолженность составляет ${money(data, 'debt')} руб. Досудебный порядок соблюдён: ${val(data, 'pretrial')}.`)}
      <div class="legal-ref">На основании ст. 309, 310, 486, 516 ГК РФ, ст. 4, 125–126 АПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика задолженность по договору поставки в размере ${money(data, 'debt')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  },

  'claim-arb-debt-services': {
    category: 'claim',
    title: 'О взыскании задолженности по договору услуг (АПК, ИП/ООО)',
    fields: [
      { name: 'court', label: 'Арбитражный суд', type: 'text', placeholder: 'Арбитражный суд ______ области/края/г. Москвы' },
      { name: 'plaintiff', label: 'Истец (ИП/ООО/ФИО)', type: 'text', placeholder: 'ИП Иванов И.И.' },
      { name: 'plaintiff_inn', label: 'ИНН истца (если есть)', type: 'text', placeholder: '__________' },
      { name: 'plaintiff_address', label: 'Адрес истца', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ООО/ИП)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'defendant_inn', label: 'ИНН ответчика (если известен)', type: 'text', placeholder: '__________' },
      { name: 'defendant_address', label: 'Адрес ответчика', type: 'text' },
      { name: 'price', label: 'Цена иска (руб.)', type: 'number', placeholder: '100000' },
      { name: 'contract', label: 'Договор услуг (№, дата)', type: 'text' },
      { name: 'debt', label: 'Задолженность (руб.)', type: 'number' },
      { name: 'acts', label: 'Акты/подтверждение оказания', type: 'text', placeholder: 'акт №__ от __.__.____' },
      { name: 'pretrial', label: 'Претензия (дата/исх.)', type: 'text' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Истец: ${val(data, 'plaintiff')} (ИНН ${val(data, 'plaintiff_inn')})`,
        `Адрес: ${val(data, 'plaintiff_address')}`,
        `Ответчик: ${val(data, 'defendant')} (ИНН ${val(data, 'defendant_inn')})`,
        `Адрес: ${val(data, 'defendant_address')}`,
        `Цена иска: ${money(data, 'price')} руб.`
      ])}
      <div class="doc-title">ИСКОВОЕ ЗАЯВЛЕНИЕ (АПК РФ)<br>о взыскании задолженности по договору услуг</div>
      ${p(`Договор: ${val(data, 'contract')}. Услуги оказаны истцом надлежащим образом, что подтверждается ${val(data, 'acts')}.`)}
      ${p(`Ответчик оплату не произвёл, задолженность составляет ${money(data, 'debt')} руб. Досудебный порядок: ${val(data, 'pretrial')}.`)}
      <div class="legal-ref">На основании ст. 309, 310, 781 ГК РФ, ст. 4, 125–126 АПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Взыскать с ответчика задолженность по договору услуг в размере ${money(data, 'debt')} руб.`,
        `Взыскать судебные расходы.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'plaintiff')} _________________`)}
    `
  }
};

  // ==================== ВОЗРАЖЕНИЯ (objection) ====================
  'objection-alimony': {
    category: 'objection',
    title: 'Возражение на иск о взыскании алиментов',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text', placeholder: 'Курганский городской суд' },
      { name: 'case_number', label: 'Номер дела', type: 'text', placeholder: '2-1234/2024' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text', placeholder: 'Петрова Анна Сергеевна' },
      { name: 'objection_text', label: 'Текст возражения', type: 'textarea', placeholder: 'Изложите суть возражения...' }
    ],
    template: (data) => `
      <div class="court-line">В ${data.court || '_______________'}</div>
      <div class="party-line">Дело № ${data.case_number || '_______________'}</div>
      <div class="party-line">Ответчик: ${data.defendant || '_______________'}</div>
      <div class="party-line">Истец: ${data.plaintiff || '_______________'}</div>
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      <div class="text-block">${data.objection_text || '_______________'}</div>
      <div class="legal-ref">На основании изложенного, руководствуясь ст. 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      <div class="request-item">1. В удовлетворении исковых требований отказать.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">${data.defendant || '_______________'} _________________</td></tr></table>
      </div>
    `
  },
  'objection-debt': {
    category: 'objection',
    title: 'Возражение на иск о взыскании долга',
    fields: [
      { name: 'court', label: 'Наименование суда', type: 'text', placeholder: 'Арбитражный суд г. Кургана' },
      { name: 'case_number', label: 'Номер дела', type: 'text', placeholder: 'А40-12345/2024' },
      { name: 'defendant', label: 'Ответчик (наименование)', type: 'text', placeholder: 'ООО "Должник"' },
      { name: 'plaintiff', label: 'Истец (наименование)', type: 'text', placeholder: 'ООО "Кредитор"' },
      { name: 'objection_text', label: 'Текст возражения', type: 'textarea', placeholder: 'Изложите суть возражения...' }
    ],
    template: (data) => `
      <div class="court-line">В ${data.court || '_______________'}</div>
      <div class="party-line">Дело № ${data.case_number || '_______________'}</div>
      <div class="party-line">Ответчик: ${data.defendant || '_______________'}</div>
      <div class="party-line">Истец: ${data.plaintiff || '_______________'}</div>
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      <div class="text-block">${data.objection_text || '_______________'}</div>
      <div class="legal-ref">На основании изложенного, руководствуясь ст. 131 АПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      <div class="request-item">1. В удовлетворении исковых требований отказать полностью.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">${data.defendant || '_______________'} _________________</td></tr></table>
      </div>
    `
  },
  'objection-loan': {
    category: 'objection',
    title: 'Возражение на иск о взыскании долга по займу',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'plaintiff', label: 'Истец (ФИО)', type: 'text' },
      { name: 'position', label: 'Позиция ответчика (кратко)', type: 'textarea', placeholder: 'Напр.: займ не получал/возвратил/срок исковой давности/неверный расчёт...' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`С исковыми требованиями не согласен(на) по следующим основаниям: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 35, 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований полностью/частично.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-receipt': {
    category: 'objection',
    title: 'Возражение на иск по расписке (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'objection_text', label: 'Суть возражений', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'defendant')}`, `Истец: ${val(data, 'plaintiff')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`${val(data, 'objection_text')}`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-consumer': {
    category: 'objection',
    title: 'Возражение на иск потребителя (ЗоЗПП) (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant_org', label: 'Ответчик (организация)', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (качество, сроки, экспертиза, злоупотребление и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'defendant_org')}`, `Истец: ${val(data, 'plaintiff')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`Возражаю против заявленных требований: ${val(data, 'position')}.`)}
      ${p(`Прошу учесть доказательства ответчика (переписка, акты, результаты проверки качества/экспертизы, расчёты).`)}
      <div class="legal-ref">Руководствуясь ст. 56, 67, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении иска полностью/частично.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Представитель/руководитель _________________`)}
    `
  },
  'objection-salary': {
    category: 'objection',
    title: 'Возражение на иск о взыскании зарплаты (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'employer', label: 'Ответчик (работодатель)', type: 'text' },
      { name: 'employee', label: 'Истец (работник)', type: 'text' },
      { name: 'position', label: 'Позиция работодателя (выплачено/расчёт неверен/пропуск срока и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'employer')}`, `Истец: ${val(data, 'employee')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`${val(data, 'position')}`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Представитель ${val(data, 'employer')} _________________`)}
    `
  },
  'objection-reinstatement': {
    category: 'objection',
    title: 'Возражение на иск о восстановлении на работе (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'employer', label: 'Ответчик (работодатель)', type: 'text' },
      { name: 'employee', label: 'Истец (работник)', type: 'text' },
      { name: 'position', label: 'Позиция (основание увольнения, соблюдение процедуры и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'employer')}`, `Истец: ${val(data, 'employee')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`С иском не согласны: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении заявленных требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Представитель ${val(data, 'employer')} _________________`)}
    `
  },
  'objection-eviction': {
    category: 'objection',
    title: 'Возражение на иск о выселении (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (право пользования, соглашение, дети и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'defendant')}`, `Истец: ${val(data, 'plaintiff')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`${val(data, 'position')}`)}
      <div class="legal-ref">Руководствуясь ст. 35, 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-unjust-enrichment': {
    category: 'objection',
    title: 'Возражение на иск о неосновательном обогащении (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (основание получения, возврат, зачёт и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'defendant')}`, `Истец: ${val(data, 'plaintiff')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`${val(data, 'position')}`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении заявленных требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-penalty': {
    category: 'objection',
    title: 'Возражение на иск о взыскании неустойки (ст. 333 ГК РФ) (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (несоразмерность, частичное исполнение и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Дело № ${val(data, 'case_number')}`, `Ответчик: ${val(data, 'defendant')}`, `Истец: ${val(data, 'plaintiff')}`])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`${val(data, 'position')}`)}
      ${p(`Дополнительно прошу применить ст. 333 ГК РФ и снизить неустойку как явно несоразмерную последствиям нарушения обязательства.`)}
      <div class="legal-ref">Руководствуясь ст. 333 ГК РФ, ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Отказать в удовлетворении требований либо снизить размер неустойки.`,
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-alimony-reduce': {
    category: 'objection',
    title: 'Возражение на иск о взыскании алиментов / уменьшение размера (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик (плательщик)', type: 'text' },
      { name: 'plaintiff', label: 'Истец (получатель)', type: 'text' },
      { name: 'position', label: 'Позиция (доход, иждивенцы, расходы, соглашение и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`Возражаю против заявленных требований/размера алиментов по следующим основаниям: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 81–83 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении иска полностью/частично либо определить иной размер алиментов с учётом обстоятельств.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-divorce': {
    category: 'objection',
    title: 'Возражение/позиция по иску о расторжении брака (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (согласен/не согласен, срок на примирение)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ (ПОЯСНЕНИЯ) ПО ДЕЛУ</div>
      ${p(`Моя позиция: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 35, 149 ГПК РФ, ст. 22 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Принять возражения/пояснения и разрешить спор с учётом изложенного.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-division-spouses': {
    category: 'objection',
    title: 'Возражение на иск о разделе имущества супругов (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (что не совместно нажитое, доли, оценки)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`С требованиями не согласен(на): ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 34, 38–39 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении требований полностью/частично либо определить иной порядок раздела.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-child-residence': {
    category: 'objection',
    title: 'Возражение на иск об определении места жительства ребёнка (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (условия, привязанность, участие)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`Возражаю: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 65 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении требований либо определить место жительства ребёнка иначе (по обстоятельствам).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-child-communication': {
    category: 'objection',
    title: 'Возражение на иск об определении порядка общения с ребёнком (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (график, безопасность, интересы ребёнка)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`Позиция: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 66 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Определить порядок общения с учётом интересов ребёнка и изложенной позиции / отказать в части требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-paternity': {
    category: 'objection',
    title: 'Возражение по иску об установлении отцовства (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (обстоятельства, ходатайство о экспертизе ДНК)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСКОВОЕ ЗАЯВЛЕНИЕ</div>
      ${p(`Возражения: ${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 79, 149 ГПК РФ, ст. 49 СК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении иска / назначить генетическую экспертизу (по ситуации).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-inheritance': {
    category: 'objection',
    title: 'Возражение на иск о восстановлении срока принятия наследства (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'interested', label: 'Заинтересованное лицо/ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец/заявитель', type: 'text' },
      { name: 'position', label: 'Позиция (неуважительность причин, пропуск срока и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Заинтересованное лицо: ${val(data, 'interested')}`,
        `Заявитель: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 1155 ГК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении заявления/иска.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'interested')} _________________`)}
    `
  },
  'objection-flood': {
    category: 'objection',
    title: 'Возражение на иск о возмещении ущерба от залива (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (вина УК/общедомовое, размер ущерба, экспертиза)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСК</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении требований полностью/частично / назначить экспертизу (по ситуации).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-defamation': {
    category: 'objection',
    title: 'Возражение на иск о защите чести и достоинства (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (оценочное суждение, истина, отсутствие распространения)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСК</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 152 ГК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении иска полностью/частично.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-395': {
    category: 'objection',
    title: 'Возражение на иск о взыскании процентов по ст. 395 ГК РФ',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (нет просрочки, неверный расчёт, зачёт)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ, ст. 395 ГК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении требований либо уменьшить сумму исходя из правильного расчёта.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-utility': {
    category: 'objection',
    title: 'Возражение на иск по ЖКУ/перерасчёту (типовой)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик (УК/РСО или гражданин)', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'position', label: 'Позиция (основания начислений, акты, показания)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении заявленных требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-arb-supply': {
    category: 'objection',
    title: 'Отзыв (возражение) на иск в арбитраж по поставке (АПК)',
    fields: [
      { name: 'court', label: 'Арбитражный суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'position', label: 'Позиция (оплата, зачёт, недопоставка, претензии)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Ответчик: ${val(data, 'defendant')}`
      ])}
      <div class="doc-title">ОТЗЫВ НА ИСКОВОЕ ЗАЯВЛЕНИЕ (АПК РФ)</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 65, 131 АПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований полностью/частично.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-arb-services': {
    category: 'objection',
    title: 'Отзыв на иск в арбитраж по услугам/подряду (АПК)',
    fields: [
      { name: 'court', label: 'Арбитражный суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'position', label: 'Позиция (акты не подписаны, недостатки, сроки)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Ответчик: ${val(data, 'defendant')}`
      ])}
      <div class="doc-title">ОТЗЫВ НА ИСК (АПК РФ)</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 65, 131 АПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении иска полностью/частично.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-arb-unjust': {
    category: 'objection',
    title: 'Отзыв на иск о неосновательном обогащении (АПК)',
    fields: [
      { name: 'court', label: 'Арбитражный суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'plaintiff', label: 'Истец', type: 'text' },
      { name: 'defendant', label: 'Ответчик', type: 'text' },
      { name: 'position', label: 'Позиция (основание получения, возврат, зачёт)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Истец: ${val(data, 'plaintiff')}`,
        `Ответчик: ${val(data, 'defendant')}`
      ])}
      <div class="doc-title">ОТЗЫВ НА ИСК (АПК РФ)</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 65, 131 АПК РФ, ст. 1102 ГК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении исковых требований.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'objection-bank-credit': {
    category: 'objection',
    title: 'Возражение на иск банка по кредиту (типовой, ГПК/АПК по ситуации)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'defendant', label: 'Ответчик (заёмщик)', type: 'text' },
      { name: 'plaintiff', label: 'Истец (банк)', type: 'text' },
      { name: 'position', label: 'Позиция (срок давности, расчёт, комиссии, штрафы, 333 ГК)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'court')}`,
        `Дело № ${val(data, 'case_number')}`,
        `Ответчик: ${val(data, 'defendant')}`,
        `Истец: ${val(data, 'plaintiff')}`
      ])}
      <div class="doc-title">ВОЗРАЖЕНИЕ НА ИСК</div>
      ${p(`${val(data, 'position')}.`)}
      <div class="legal-ref">Руководствуясь ст. 56, 149 ГПК РФ (или ст. 131 АПК РФ), ст. 333 ГК РФ (по ситуации),</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отказать в удовлетворении требований полностью/частично / снизить неустойку по ст. 333 ГК РФ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },

  // ==================== ЖАЛОБЫ (complaint) ====================
  'complaint-fas': {
    category: 'complaint',
    title: 'Жалоба в ФАС',
    fields: [
      { name: 'fas_department', label: 'Управление ФАС', type: 'text', placeholder: 'Московское УФАС России' },
      { name: 'applicant', label: 'Заявитель (наименование)', type: 'text' },
      { name: 'violation_desc', label: 'Описание нарушения антимонопольного законодательства', type: 'textarea' }
    ],
    template: (data) => `
      <div class="court-line">В ${data.fas_department || '_______________'}</div>
      <div class="party-line">от ${data.applicant || '_______________'}</div>
      <div class="doc-title">ЖАЛОБА</div>
      <div class="text-block">${data.violation_desc || '_______________'}</div>
      <div class="legal-ref">На основании Федерального закона «О защите конкуренции»,</div>
      <div class="request-title">ПРОШУ:</div>
      <div class="request-item">1. Провести проверку по факту нарушения.</div>
      <div class="request-item">2. Выдать предписание об устранении нарушения.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Подпись: ___________</td></tr></table>
      </div>
    `
  },
  'complaint-prosecutor': {
    category: 'complaint',
    title: 'Жалоба в прокуратуру (типовая)',
    fields: [
      { name: 'prosecutor_office', label: 'Прокуратура (наименование)', type: 'text', placeholder: 'Прокуратура ______ района г. ______' },
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'complaint_text', label: 'Суть нарушения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что проверить/принять меры)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([
        `В ${val(data, 'prosecutor_office')}`,
        `от ${val(data, 'applicant')}`,
        `адрес: ${val(data, 'address')}`,
        `тел.: ${val(data, 'phone')}`
      ])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'complaint_text')}`)}
      <div class="legal-ref">На основании Федерального закона «О прокуратуре Российской Федерации»,</div>
      <div class="request-title">ПРОШУ:</div>
      ${p(`${val(data, 'requests')}`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-police-inaction': {
    category: 'complaint',
    title: 'Жалоба на бездействие полиции (КУСП/проверка)',
    fields: [
      { name: 'to', label: 'Куда (начальнику ОМВД/прокуратуре)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'kusp', label: 'КУСП/номер материала', type: 'text' },
      { name: 'statement_date', label: 'Дата обращения', type: 'date' },
      { name: 'facts', label: 'Факты бездействия', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА НА БЕЗДЕЙСТВИЕ</div>
      ${p(`Я обратился(ась) с заявлением ${dateRu(data.statement_date)}. Материал: ${val(data, 'kusp')}. В настоящее время необходимые действия не выполняются: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании ст. 124–125 УПК РФ (по ситуации),</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Провести проверку доводов жалобы.`,
        `Организовать проведение процессуальной проверки и принять решение в установленный срок.`,
        `Сообщить о результатах рассмотрения.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-fssp-inaction': {
    category: 'complaint',
    title: 'Жалоба на бездействие судебного пристава-исполнителя',
    fields: [
      { name: 'to', label: 'Куда (начальнику ОСП/УФССП)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'ip_number', label: 'Номер ИП', type: 'text' },
      { name: 'facts', label: 'Что не делает пристав', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `по ИП № ${val(data, 'ip_number')}`])}
      <div class="doc-title">ЖАЛОБА НА БЕЗДЕЙСТВИЕ СУДЕБНОГО ПРИСТАВА</div>
      ${p(`${val(data, 'facts')}`)}
      <div class="legal-ref">На основании ФЗ «Об исполнительном производстве»,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Признать бездействие незаконным (по результатам проверки).`,
        `Обязать пристава совершить исполнительные действия.`,
        `Сообщить о принятых мерах.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-rospotrebnadzor': {
    category: 'complaint',
    title: 'Жалоба в Роспотребнадзор',
    fields: [
      { name: 'to', label: 'Территориальный орган Роспотребнадзора', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'seller', label: 'Продавец/исполнитель', type: 'text' },
      { name: 'facts', label: 'Нарушение', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Прошу провести проверку в отношении ${val(data, 'seller')} по факту: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании Закона РФ «О защите прав потребителей»,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([
        `Провести проверку и при наличии оснований принять меры реагирования.`,
        `Сообщить о результатах.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-git': {
    category: 'complaint',
    title: 'Жалоба в Государственную инспекцию труда',
    fields: [
      { name: 'to', label: 'ГИТ (регион)', type: 'text' },
      { name: 'employee', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'employer', label: 'Работодатель', type: 'text' },
      { name: 'facts', label: 'Нарушение трудовых прав', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Прошу провести проверку работодателя ${val(data, 'employer')} по факту: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании ТК РФ и ФЗ № 59-ФЗ «О порядке рассмотрения обращений граждан РФ»,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и выдать предписание об устранении нарушений (при наличии оснований).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'complaint-housing-inspection': {
    category: 'complaint',
    title: 'Жалоба в Госжилинспекцию (управляющая компания)',
    fields: [
      { name: 'to', label: 'ГЖИ (регион)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'house', label: 'Адрес дома', type: 'text' },
      { name: 'uk', label: 'УК/ТСЖ', type: 'text' },
      { name: 'facts', label: 'Нарушение (услуги ЖКХ)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Дом: ${val(data, 'house')}. Управляющая организация: ${val(data, 'uk')}. Нарушение: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании ЖК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и обязать УК устранить нарушения (при наличии оснований).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-roskomnadzor-personaldata': {
    category: 'complaint',
    title: 'Жалоба в Роскомнадзор (персональные данные)',
    fields: [
      { name: 'to', label: 'Роскомнадзор (управление)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'operator', label: 'Оператор ПДн (организация)', type: 'text' },
      { name: 'facts', label: 'Нарушение обработки ПДн', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Прошу провести проверку соблюдения требований ФЗ «О персональных данных» оператором ${val(data, 'operator')} по факту: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании ФЗ № 152-ФЗ «О персональных данных»,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Принять меры реагирования и сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-cbr-bank': {
    category: 'complaint',
    title: 'Жалоба в Банк России (на банк/МФО)',
    fields: [
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'bank', label: 'Банк/МФО', type: 'text' },
      { name: 'facts', label: 'Суть нарушения', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В Банк России`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Прошу рассмотреть обращение в отношении ${val(data, 'bank')}: ${val(data, 'facts')}.`)}
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку в рамках компетенции и сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-court-chairman-delay': {
    category: 'complaint',
    title: 'Жалоба председателю суда (затягивание/организация процесса)',
    fields: [
      { name: 'to', label: 'Председателю суда (наименование)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'facts', label: 'Суть (затягивание, не выдаются копии и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`Председателю ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `по делу № ${val(data, 'case_number')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Принять меры по организации рассмотрения дела/доступа к материалам в пределах полномочий.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-finombudsman': {
    category: 'complaint',
    title: 'Обращение финансовому уполномоченному (страховая/банк/МФО) (типовое)',
    fields: [
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'org', label: 'Финансовая организация', type: 'text', placeholder: 'страховая/банк/МФО' },
      { name: 'product', label: 'Договор/услуга (полис/кредит)', type: 'text' },
      { name: 'facts', label: 'Суть спора', type: 'textarea' },
      { name: 'requests', label: 'Требования', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`Финансовому уполномоченному`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ОБРАЩЕНИЕ</div>
      ${p(`Финансовая организация: ${val(data, 'org')}. Продукт/договор: ${val(data, 'product')}.`)}
      ${p(`Суть: ${val(data, 'facts')}.`)}
      ${p(`Требования: ${val(data, 'requests')}.`)}
      <div class="legal-ref">ФЗ № 123-ФЗ «Об уполномоченном по правам потребителей финансовых услуг».</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть обращение и принять решение в рамках компетенции.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-rsa-osago': {
    category: 'complaint',
    title: 'Жалоба в РСА по ОСАГО (типовая)',
    fields: [
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'insurer', label: 'Страховая компания', type: 'text' },
      { name: 'policy', label: 'Полис ОСАГО (номер)', type: 'text' },
      { name: 'facts', label: 'Нарушение (отказ, навязывание, КБМ и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В Российский Союз Автостраховщиков (РСА)`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Страховая компания: ${val(data, 'insurer')}, полис: ${val(data, 'policy')}.`)}
      ${p(`Суть: ${val(data, 'facts')}.`)}
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку в рамках компетенции и сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-fns': {
    category: 'complaint',
    title: 'Жалоба в ФНС (налоговый орган) (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`, `тел.: ${val(data, 'phone')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ «О порядке рассмотрения обращений граждан РФ».</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть жалобу и сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-sfr': {
    category: 'complaint',
    title: 'Жалоба в Социальный фонд России (СФР) (пособия/пенсия) (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (СФР)`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`, `тел.: ${val(data, 'phone')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ.</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть обращение и предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-roszdravnadzor': {
    category: 'complaint',
    title: 'Жалоба в Росздравнадзор (медицинская помощь/лекарства)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (Росздравнадзор)`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`, `тел.: ${val(data, 'phone')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ; ФЗ № 323-ФЗ «Об основах охраны здоровья граждан в РФ» (по ситуации).</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и принять меры реагирования.`, `Сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-mvd-migration': {
    category: 'complaint',
    title: 'Жалоба в подразделение по вопросам миграции МВД (паспорт/регистрация) (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (МВД, по вопросам миграции)`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ.</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть обращение и устранить нарушения (при наличии оснований).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-ombudsman': {
    category: 'complaint',
    title: 'Жалоба Уполномоченному по правам человека (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`Уполномоченному по правам человека`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`])}
      <div class="doc-title">ОБРАЩЕНИЕ (ЖАЛОБА)</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть обращение и содействовать восстановлению прав в пределах полномочий.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-administration': {
    category: 'complaint',
    title: 'Жалоба в администрацию (двор/благоустройство/шум) (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (администрация)`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ.</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и принять меры.`, `Сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-fssp-actions': {
    category: 'complaint',
    title: 'Жалоба на действия/постановление пристава (а не на бездействие)',
    fields: [
      { name: 'to', label: 'Куда (начальнику ОСП/УФССП/суд)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'ip_number', label: 'Номер ИП', type: 'text' },
      { name: 'action', label: 'Какое действие/постановление обжалуете', type: 'textarea' },
      { name: 'requests', label: 'Что просите', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `по ИП № ${val(data, 'ip_number')}`])}
      <div class="doc-title">ЖАЛОБА НА ДЕЙСТВИЯ/ПОСТАНОВЛЕНИЕ СУДЕБНОГО ПРИСТАВА</div>
      ${p(`Обжалуемое: ${val(data, 'action')}.`)}
      ${p(`Прошу: ${val(data, 'requests')}.`)}
      <div class="legal-ref">ФЗ «Об исполнительном производстве».</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть жалобу и принять решение в установленный срок.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-rkn-spam-calls': {
    category: 'complaint',
    title: 'Жалоба в Роскомнадзор (навязчивые звонки/реклама/обработка ПДн)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (Роскомнадзор)`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 152-ФЗ «О персональных данных»; ФЗ «О рекламе» (по ситуации).</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и принять меры.`, `Сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-bank-collection': {
    category: 'complaint',
    title: 'Жалоба в ФССП на коллекторов/взыскателей (нарушение 230-ФЗ)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (ФССП)`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 230-ФЗ (по ситуации) и ФЗ № 59-ФЗ.</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и привлечь виновных к ответственности (при наличии оснований).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-rpn-ecology': {
    category: 'complaint',
    title: 'Жалоба в Росприроднадзор (запахи/сбросы/мусор) (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')} (Росприроднадзор)`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ.</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и принять меры реагирования.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-radnadzor-consumer-credit': {
    category: 'complaint',
    title: 'Жалоба в Банк России (навязывание услуг/страховки по кредиту) (типовая)',
    fields: [
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'bank', label: 'Банк', type: 'text' },
      { name: 'contract', label: 'Договор (№/дата)', type: 'text' },
      { name: 'facts', label: 'Суть нарушения', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В Банк России`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`Банк: ${val(data, 'bank')}, договор: ${val(data, 'contract')}.`)}
      ${p(`${val(data, 'facts')}`)}
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть обращение и принять меры в рамках компетенции.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-gosuslugi-operator': {
    category: 'complaint',
    title: 'Жалоба на действия МФЦ/органа при оказании госуслуги (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 210-ФЗ «Об организации предоставления государственных и муниципальных услуг» (по ситуации).</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Рассмотреть жалобу и устранить нарушения.`, `Сообщить о результатах.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'complaint-education': {
    category: 'complaint',
    title: 'Жалоба в орган управления образованием/Рособрнадзор (типовая)',
    fields: [
      { name: 'to', label: 'Куда (орган/должностное лицо)', type: 'text', placeholder: 'В ______' },
      { name: 'applicant', label: 'Заявитель (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес заявителя', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Суть обращения', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (что просите)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЖАЛОБА</div>
      ${p(`${val(data, 'facts')}`)}
      ${p(`Прошу: ${val(data, 'requests')}`)}
      <div class="legal-ref">ФЗ № 59-ФЗ; ФЗ «Об образовании в РФ» (по ситуации).</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Провести проверку и принять меры.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },

  // ==================== ПРЕТЕНЗИИ (pretense) ====================
  'pretense-quality': {
    category: 'pretense',
    title: 'Претензия о ненадлежащем качестве услуги',
    fields: [
      { name: 'recipient', label: 'Исполнитель (название)', type: 'text' },
      { name: 'sender', label: 'Заказчик (ФИО)', type: 'text' },
      { name: 'service', label: 'Наименование услуги', type: 'text' },
      { name: 'price', label: 'Стоимость услуги', type: 'number' },
      { name: 'defect_description', label: 'Описание недостатков', type: 'textarea' }
    ],
    template: (data) => `
      <div class="court-line">${data.recipient || '_______________'}</div>
      <div class="party-line">от ${data.sender || '_______________'}</div>
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      <div class="text-block">Мною была заказана услуга: ${data.service || '_______________'} стоимостью ${data.price || '_______'} руб. При оказании услуги выявлены недостатки: ${data.defect_description || '_______________'}.</div>
      <div class="text-block">На основании ст. 29 Закона «О защите прав потребителей» прошу устранить недостатки или уменьшить цену.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Подпись: ___________</td></tr></table>
      </div>
    `
  },
  'pretense-goods-refund': {
    category: 'pretense',
    title: 'Претензия о возврате денег за товар ненадлежащего качества',
    fields: [
      { name: 'seller', label: 'Продавец (организация)', type: 'text' },
      { name: 'buyer', label: 'Покупатель (ФИО)', type: 'text' },
      { name: 'product', label: 'Товар', type: 'text' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'purchase_date', label: 'Дата покупки', type: 'date' },
      { name: 'defect', label: 'Недостаток', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'seller')}`, `от ${val(data, 'buyer')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`${dateRu(data.purchase_date)} мной приобретён товар: ${val(data, 'product')} стоимостью ${money(data, 'price')} руб. Выявлен недостаток: ${val(data, 'defect')}.`)}
      ${p(`На основании Закона РФ «О защите прав потребителей» прошу вернуть уплаченную сумму в течение установленного законом срока.`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([
        `Вернуть ${money(data, 'price')} руб.`,
        `Сообщить о времени и месте передачи товара (при необходимости).`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-delivery-delay': {
    category: 'pretense',
    title: 'Претензия о нарушении срока доставки/оказания услуги',
    fields: [
      { name: 'recipient', label: 'Исполнитель/продавец', type: 'text' },
      { name: 'sender', label: 'Потребитель (ФИО)', type: 'text' },
      { name: 'order', label: 'Заказ/договор (номер)', type: 'text' },
      { name: 'due', label: 'Срок исполнения (дата/период)', type: 'text' },
      { name: 'facts', label: 'Что нарушено', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По заказу/договору ${val(data, 'order')} срок исполнения: ${val(data, 'due')}. Нарушение: ${val(data, 'facts')}.`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([
        `Исполнить обязательство в разумный срок и сообщить дату исполнения.`,
        `Уплатить неустойку (при наличии оснований; расчёт может быть представлен дополнительно).`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-rent-deposit': {
    category: 'pretense',
    title: 'Претензия о возврате обеспечительного платежа (залог) по аренде',
    fields: [
      { name: 'landlord', label: 'Арендодатель', type: 'text' },
      { name: 'tenant', label: 'Арендатор', type: 'text' },
      { name: 'address', label: 'Адрес объекта', type: 'text' },
      { name: 'deposit', label: 'Сумма обеспечительного платежа (руб.)', type: 'number' },
      { name: 'moveout_date', label: 'Дата выезда/передачи', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'landlord')}`, `от ${val(data, 'tenant')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По договору аренды объект: ${val(data, 'address')}. Обеспечительный платеж: ${money(data, 'deposit')} руб. Объект передан/выезд состоялся ${dateRu(data.moveout_date)}.`)}
      ${p(`Прошу вернуть обеспечительный платеж в срок, предусмотренный договором/разумный срок.`)}
      <div class="legal-ref">Основание: ст. 309 ГК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-contractor-defects': {
    category: 'pretense',
    title: 'Претензия по договору подряда (недостатки работ)',
    fields: [
      { name: 'contractor', label: 'Подрядчик', type: 'text' },
      { name: 'customer', label: 'Заказчик', type: 'text' },
      { name: 'contract', label: 'Договор (номер/дата)', type: 'text' },
      { name: 'defects', label: 'Недостатки работ', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'contractor')}`, `от ${val(data, 'customer')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По договору ${val(data, 'contract')} выявлены недостатки выполненных работ: ${val(data, 'defects')}.`)}
      <div class="legal-ref">На основании ст. 723 ГК РФ,</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([
        `Безвозмездно устранить недостатки в разумный срок / соразмерно уменьшить цену / возместить расходы на устранение (выбрать подходящее).`,
        `Предоставить ответ в письменной форме.`
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-insurance-payment': {
    category: 'pretense',
    title: 'Претензия в страховую о страховой выплате',
    fields: [
      { name: 'insurer', label: 'Страховая компания', type: 'text' },
      { name: 'insured', label: 'Заявитель', type: 'text' },
      { name: 'policy', label: 'Полис (номер)', type: 'text' },
      { name: 'event_date', label: 'Дата страхового случая', type: 'date' },
      { name: 'facts', label: 'Суть отказа/задержки', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'insurer')}`, `от ${val(data, 'insured')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Полис: ${val(data, 'policy')}. Страховой случай: ${dateRu(data.event_date)}. ${val(data, 'facts')}.`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([
        `Произвести страховую выплату / выдать мотивированный письменный ответ с указанием норм и обстоятельств.`,
      ])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-telecom': {
    category: 'pretense',
    title: 'Претензия оператору связи (качество/списания)',
    fields: [
      { name: 'operator', label: 'Оператор связи', type: 'text' },
      { name: 'subscriber', label: 'Абонент', type: 'text' },
      { name: 'number', label: 'Номер/лицевой счёт', type: 'text' },
      { name: 'facts', label: 'Проблема', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'operator')}`, `от ${val(data, 'subscriber')}`, `лиц. счёт/номер: ${val(data, 'number')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`${val(data, 'facts')}`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Провести проверку, устранить нарушения и произвести перерасчёт (при наличии оснований).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-tourism': {
    category: 'pretense',
    title: 'Претензия туроператору/турагенту (качество туристского продукта)',
    fields: [
      { name: 'company', label: 'Туроператор/турагент', type: 'text' },
      { name: 'client', label: 'Клиент', type: 'text' },
      { name: 'contract', label: 'Договор (номер/дата)', type: 'text' },
      { name: 'facts', label: 'Нарушения', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'company')}`, `от ${val(data, 'client')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По договору ${val(data, 'contract')} выявлены нарушения: ${val(data, 'facts')}.`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Компенсировать убытки/уменьшить цену/вернуть денежные средства (по ситуации) и предоставить ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-neighbor-flood': {
    category: 'pretense',
    title: 'Претензия соседу о возмещении ущерба (залив квартиры)',
    fields: [
      { name: 'recipient', label: 'Кому (ФИО соседа)', type: 'text' },
      { name: 'sender', label: 'От кого (ФИО)', type: 'text' },
      { name: 'date', label: 'Дата залива', type: 'date' },
      { name: 'damage', label: 'Ущерб (руб.)', type: 'number' },
      { name: 'facts', label: 'Описание и доказательства (акт, фото)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`В результате залива ${dateRu(data.date)} причинён ущерб на сумму ${money(data, 'damage')} руб. Обстоятельства: ${val(data, 'facts')}.`)}
      <div class="legal-ref">На основании ст. 1064 ГК РФ прошу возместить ущерб добровольно.</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Возместить ${money(data, 'damage')} руб. в срок ____ дней с даты получения претензии.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-employer-pay': {
    category: 'pretense',
    title: 'Претензия работодателю о выплате задолженности (досудебно)',
    fields: [
      { name: 'employer', label: 'Работодатель', type: 'text' },
      { name: 'employee', label: 'Работник', type: 'text' },
      { name: 'period', label: 'Период', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'employer')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Прошу выплатить задолженность по заработной плате за период ${val(data, 'period')} в размере ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Основание: ст. 136, 236 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-consumer-exchange': {
    category: 'pretense',
    title: 'Претензия: обмен/возврат товара надлежащего качества (14 дней) (ЗоЗПП)',
    fields: [
      { name: 'recipient', label: 'Продавец (ООО/магазин)', type: 'text' },
      { name: 'sender', label: 'Покупатель (ФИО)', type: 'text' },
      { name: 'product', label: 'Товар', type: 'text' },
      { name: 'purchase_date', label: 'Дата покупки', type: 'date' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'reason', label: 'Причина (не подошёл размер/цвет и т.п.)', type: 'text' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`${dateRu(data.purchase_date)} приобретён товар: ${val(data, 'product')} стоимостью ${money(data, 'price')} руб. Товар надлежащего качества, однако ${val(data, 'reason')}.`)}
      ${p(`Прошу произвести обмен на аналогичный товар либо вернуть денежные средства (при отсутствии подходящего товара).`)}
      <div class="legal-ref">Основание: ст. 25 Закона РФ «О защите прав потребителей».</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Обменять товар либо вернуть ${money(data, 'price')} руб. в установленный законом срок.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-consumer-repair': {
    category: 'pretense',
    title: 'Претензия: требование устранить недостатки товара (гарантийный ремонт) (ЗоЗПП)',
    fields: [
      { name: 'recipient', label: 'Продавец/сервис', type: 'text' },
      { name: 'sender', label: 'Покупатель', type: 'text' },
      { name: 'product', label: 'Товар', type: 'text' },
      { name: 'purchase_date', label: 'Дата покупки', type: 'date' },
      { name: 'defect', label: 'Недостаток', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Товар: ${val(data, 'product')}, куплен ${dateRu(data.purchase_date)}. Недостаток: ${val(data, 'defect')}.`)}
      ${p(`Прошу безвозмездно устранить недостатки в установленный законом срок.`)}
      <div class="legal-ref">Основание: ст. 18, 20 Закона РФ «О защите прав потребителей».</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Принять товар и выполнить гарантийный ремонт.`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-consumer-refund-service': {
    category: 'pretense',
    title: 'Претензия: возврат денег за услугу (ненадлежащее оказание) (ЗоЗПП)',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`, `${val(data, 'address')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Договор/заказ: ${val(data, 'contract')}.`)}
      ${p(`Нарушение: ${val(data, 'facts')}.`)}
      ${p(`Требую вернуть/уменьшить стоимость: ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Основание: ст. 29, 31 Закона РФ «О защите прав потребителей».</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Вернуть ${money(data, 'amount')} руб. в установленный законом срок.`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-internet-shop-no-delivery': {
    category: 'pretense',
    title: 'Претензия: интернет-магазин — не доставлен оплаченный товар',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' },
      { name: 'pay_date', label: 'Дата оплаты', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Оплата по заказу ${val(data, 'contract')} произведена ${dateRu(data.pay_date)}. Товар не доставлен/срок нарушен.`)}
      ${p(`Обстоятельства: ${val(data, 'facts')}.`)}
      <div class="legal-ref">Основание: ст. 309 ГК РФ; Закон РФ «О защите прав потребителей» (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Поставить товар в срок либо вернуть ${money(data, 'amount')} руб.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-ddU-214fz-penalty': {
    category: 'pretense',
    title: 'Претензия застройщику: неустойка за просрочку передачи квартиры (214-ФЗ)',
    fields: [
      { name: 'recipient', label: 'Застройщик (ООО)', type: 'text' },
      { name: 'sender', label: 'Дольщик (ФИО)', type: 'text' },
      { name: 'ddu', label: 'ДДУ (№, дата)', type: 'text' },
      { name: 'due_date', label: 'Срок передачи по ДДУ', type: 'date' },
      { name: 'calc', label: 'Расчёт неустойки (текстом)', type: 'textarea' },
      { name: 'amount', label: 'Сумма неустойки (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По ДДУ ${val(data, 'ddu')} срок передачи объекта: ${dateRu(data.due_date)}. Срок нарушен.`)}
      ${p(`Расчёт неустойки: ${val(data, 'calc')}.`)}
      ${p(`Требую выплатить неустойку ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Основание: ФЗ № 214-ФЗ; ст. 309, 310 ГК РФ.</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Выплатить неустойку ${money(data, 'amount')} руб.`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-bank-unauthorized-charge': {
    category: 'pretense',
    title: 'Претензия в банк: несанкционированное списание/операция',
    fields: [
      { name: 'recipient', label: 'Банк', type: 'text' },
      { name: 'sender', label: 'Клиент (ФИО)', type: 'text' },
      { name: 'card', label: 'Карта/счёт', type: 'text' },
      { name: 'date', label: 'Дата операции', type: 'date' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'facts', label: 'Обстоятельства (не совершал, уведомлял банк)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По карте/счёту ${val(data, 'card')} ${dateRu(data.date)} списано ${money(data, 'amount')} руб. Операцию не совершал(а).`)}
      ${p(`Обстоятельства: ${val(data, 'facts')}.`)}
      <div class="legal-ref">Прошу провести расследование операции и принять меры защиты. (Нормативные основания зависят от ситуации и договора.)</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Вернуть ${money(data, 'amount')} руб. (при наличии оснований).`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-airline-delay': {
    category: 'pretense',
    title: 'Претензия авиакомпании: задержка/отмена рейса (типовая)',
    fields: [
      { name: 'recipient', label: 'Авиакомпания', type: 'text' },
      { name: 'sender', label: 'Пассажир (ФИО)', type: 'text' },
      { name: 'flight', label: 'Рейс', type: 'text' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'facts', label: 'Нарушение и расходы', type: 'textarea' },
      { name: 'amount', label: 'Сумма требований (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Рейс ${val(data, 'flight')} от ${dateRu(data.date)}. Нарушение: ${val(data, 'facts')}.`)}
      ${p(`Требую компенсировать расходы/убытки на сумму ${money(data, 'amount')} руб. (при наличии подтверждений).`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Удовлетворить требования в досудебном порядке и дать письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-gym-refund': {
    category: 'pretense',
    title: 'Претензия фитнес-клубу: возврат денежных средств по абонементу',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Договор/абонемент: ${val(data, 'contract')}.`)}
      ${p(`Обстоятельства: ${val(data, 'facts')}.`)}
      ${p(`Прошу вернуть ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Закон РФ «О защите прав потребителей» (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Вернуть ${money(data, 'amount')} руб. в установленный законом срок.`, `Предоставить ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-education-refund': {
    category: 'pretense',
    title: 'Претензия образовательной организации: возврат денежных средств за обучение',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Договор: ${val(data, 'contract')}.`)}
      ${p(`Нарушение/основание: ${val(data, 'facts')}.`)}
      ${p(`Требую возврат ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Закон РФ «О защите прав потребителей»; условия договора (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Вернуть ${money(data, 'amount')} руб.`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-medical-service': {
    category: 'pretense',
    title: 'Претензия клинике: ненадлежащие медицинские услуги (типовая)',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Договор/квитанция: ${val(data, 'contract')}.`)}
      ${p(`Описание: ${val(data, 'facts')}.`)}
      ${p(`Требую компенсацию/возврат ${money(data, 'amount')} руб. (по ситуации).`)}
      <div class="legal-ref">Закон РФ «О защите прав потребителей»; ФЗ № 323-ФЗ (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Рассмотреть претензию и дать мотивированный ответ.`, `Компенсировать убытки/вернуть оплату (по ситуации).`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-uk-property-damage': {
    category: 'pretense',
    title: 'Претензия в УК: возмещение ущерба имуществу (подъезд/кровля/общедомовое)',
    fields: [
      { name: 'recipient', label: 'Кому (ООО/организация)', type: 'text', placeholder: 'ООО «Ромашка»' },
      { name: 'sender', label: 'От кого (ФИО/ИП)', type: 'text' },
      { name: 'address', label: 'Адрес/контакты отправителя', type: 'text', placeholder: 'адрес, e-mail, телефон' },
      { name: 'contract', label: 'Договор/заказ (№, дата)', type: 'text', placeholder: '№ ___ от __.__.____' },
      { name: 'facts', label: 'Описание нарушения', type: 'textarea' },
      { name: 'amount', label: 'Сумма требования (руб.)', type: 'number', placeholder: '10000' },
      { name: 'date', label: 'Дата события', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')} (УК/ТСЖ)`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`${dateRu(data.date)} произошло событие, повлекшее ущерб (по вине/ненадлежащему содержанию общедомового имущества): ${val(data, 'facts')}.`)}
      ${p(`Прошу возместить ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">ЖК РФ; ст. 1064 ГК РФ (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Возместить ущерб ${money(data, 'amount')} руб.`, `Предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-mfo-230fz': {
    category: 'pretense',
    title: 'Претензия МФО/коллекторам: прекращение нарушений (230-ФЗ)',
    fields: [
      { name: 'recipient', label: 'Кому (МФО/коллектор)', type: 'text' },
      { name: 'sender', label: 'От кого', type: 'text' },
      { name: 'facts', label: 'Нарушения (звонки, угрозы, третьи лица)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Со стороны организации допускаются нарушения порядка взаимодействия: ${val(data, 'facts')}.`)}
      <div class="legal-ref">Основание: ФЗ № 230-ФЗ (по ситуации).</div>
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Прекратить нарушения и предоставить письменный ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-insurance-osago-payment': {
    category: 'pretense',
    title: 'Претензия в страховую по ОСАГО: доплата страхового возмещения',
    fields: [
      { name: 'recipient', label: 'Страховая компания', type: 'text' },
      { name: 'sender', label: 'Потерпевший/страхователь', type: 'text' },
      { name: 'claim', label: 'Заявление/убыток (номер)', type: 'text' },
      { name: 'facts', label: 'Суть (занижение выплаты, расчёт)', type: 'textarea' },
      { name: 'amount', label: 'Сумма доплаты (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`По убытку ${val(data, 'claim')} выплата произведена в недостаточном размере. ${val(data, 'facts')}.`)}
      ${p(`Требую доплату ${money(data, 'amount')} руб.`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Произвести доплату ${money(data, 'amount')} руб. и направить ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },
  'pretense-mobile-operator-fees': {
    category: 'pretense',
    title: 'Претензия оператору: незаконные списания/подключённые услуги',
    fields: [
      { name: 'recipient', label: 'Оператор связи', type: 'text' },
      { name: 'sender', label: 'Абонент (ФИО)', type: 'text' },
      { name: 'number', label: 'Номер', type: 'text' },
      { name: 'facts', label: 'Списания/услуги', type: 'textarea' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'recipient')}`, `от ${val(data, 'sender')}`, `номер: ${val(data, 'number')}`])}
      <div class="doc-title">ПРЕТЕНЗИЯ</div>
      ${p(`Обстоятельства: ${val(data, 'facts')}.`)}
      ${p(`Прошу отключить навязанные услуги и вернуть ${money(data, 'amount')} руб. (при наличии оснований).`)}
      <div class="request-title">ТРЕБОВАНИЯ:</div>
      ${reqBlock([`Провести проверку, сделать перерасчёт, предоставить ответ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подпись: ___________`)}
    `
  },

  // ==================== ЗАЯВЛЕНИЯ (statement) ====================
  'statement-vacation': {
    category: 'statement',
    title: 'Заявление на отпуск',
    fields: [
      { name: 'director', label: 'Директор (ФИО)', type: 'text', placeholder: 'Иванову И.И.' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Петров П.П.' },
      { name: 'position', label: 'Должность', type: 'text', placeholder: 'менеджер' },
      { name: 'start_date', label: 'Дата начала отпуска', type: 'date' },
      { name: 'end_date', label: 'Дата окончания отпуска', type: 'date' }
    ],
    template: (data) => `
      <div class="court-line">Директору ${data.director || '_______________'}</div>
      <div class="party-line">от ${data.employee || '_______________'}</div>
      <div class="party-line">должность: ${data.position || '_______________'}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      <div class="text-block">Прошу предоставить мне ежегодный оплачиваемый отпуск с ${data.start_date || '_______________'} по ${data.end_date || '_______________'}.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">${data.employee || '_______________'} _________________</td></tr></table>
      </div>
    `
  },
  'statement-resignation': {
    category: 'statement',
    title: 'Заявление на увольнение',
    fields: [
      { name: 'director', label: 'Директор (ФИО)', type: 'text', placeholder: 'Иванову И.И.' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Петров П.П.' },
      { name: 'position', label: 'Должность', type: 'text', placeholder: 'менеджер' },
      { name: 'last_day', label: 'Последний рабочий день', type: 'date' }
    ],
    template: (data) => `
      <div class="court-line">Директору ${data.director || '_______________'}</div>
      <div class="party-line">от ${data.employee || '_______________'}</div>
      <div class="party-line">должность: ${data.position || '_______________'}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      <div class="text-block">Прошу уволить меня по собственному желанию ${data.last_day || '_______________'}.</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">Дата: ${new Date().toLocaleDateString('ru-RU')}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">${data.employee || '_______________'} _________________</td></tr></table>
      </div>
    `
  },
  'statement-unpaid-leave': {
    category: 'statement',
    title: 'Заявление на отпуск без сохранения заработной платы',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ____' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text' },
      { name: 'position', label: 'Должность', type: 'text' },
      { name: 'start_date', label: 'С (дата)', type: 'date' },
      { name: 'end_date', label: 'По (дата)', type: 'date' },
      { name: 'reason', label: 'Причина (по желанию)', type: 'text' }
    ],
    template: (data) => `
      <div class="court-line">${val(data, 'director')}</div>
      <div class="party-line">от ${val(data, 'employee')}, ${val(data, 'position')}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу предоставить отпуск без сохранения заработной платы с ${dateRu(data.start_date)} по ${dateRu(data.end_date)} ${data.reason ? `по причине: ${val(data, 'reason')}.` : '.'}`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-transfer': {
    category: 'statement',
    title: 'Заявление о переводе на другую должность (по соглашению)',
    fields: [
      { name: 'director', label: 'Кому', type: 'text' },
      { name: 'employee', label: 'От кого', type: 'text' },
      { name: 'current_position', label: 'Текущая должность', type: 'text' },
      { name: 'new_position', label: 'Желаемая должность', type: 'text' },
      { name: 'date', label: 'Дата перевода', type: 'date' }
    ],
    template: (data) => `
      <div class="court-line">${val(data, 'director')}</div>
      <div class="party-line">от ${val(data, 'employee')}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу перевести меня с должности ${val(data, 'current_position')} на должность ${val(data, 'new_position')} с ${dateRu(data.date)} по соглашению сторон.`)}
      <div class="legal-ref">Основание: ст. 72.1 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-copies-docs': {
    category: 'statement',
    title: 'Заявление о выдаче копий документов, связанных с работой',
    fields: [
      { name: 'director', label: 'Кому', type: 'text' },
      { name: 'employee', label: 'От кого', type: 'text' },
      { name: 'docs', label: 'Какие документы (перечень)', type: 'textarea', placeholder: 'Трудовой договор, приказы, справки...' }
    ],
    template: (data) => `
      <div class="court-line">${val(data, 'director')}</div>
      <div class="party-line">от ${val(data, 'employee')}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу выдать мне надлежащим образом заверенные копии следующих документов, связанных с работой: ${val(data, 'docs')}.`)}
      <div class="legal-ref">Основание: ст. 62 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-issue-std-r': {
    category: 'statement',
    title: 'Заявление о выдаче СТД-Р',
    fields: [
      { name: 'director', label: 'Кому', type: 'text' },
      { name: 'employee', label: 'От кого', type: 'text' }
    ],
    template: (data) => `
      <div class="court-line">${val(data, 'director')}</div>
      <div class="party-line">от ${val(data, 'employee')}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу выдать мне сведения о трудовой деятельности по форме СТД-Р.`)}
      <div class="legal-ref">Основание: ст. 66.1 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-mat-aid': {
    category: 'statement',
    title: 'Заявление на материальную помощь (типовое)',
    fields: [
      { name: 'director', label: 'Кому', type: 'text' },
      { name: 'employee', label: 'От кого', type: 'text' },
      { name: 'reason', label: 'Основание (обстоятельства)', type: 'textarea' }
    ],
    template: (data) => `
      <div class="court-line">${val(data, 'director')}</div>
      <div class="party-line">от ${val(data, 'employee')}</div>
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу оказать материальную помощь в связи со следующими обстоятельствами: ${val(data, 'reason')}.`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-police-crime': {
    category: 'statement',
    title: 'Заявление в полицию о преступлении (типовое)',
    fields: [
      { name: 'to', label: 'Куда (ОМВД)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'address', label: 'Адрес', type: 'text' },
      { name: 'phone', label: 'Телефон', type: 'text' },
      { name: 'facts', label: 'Обстоятельства', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (возбудить дело/проверка)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `адрес: ${val(data, 'address')}`, `тел.: ${val(data, 'phone')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ О ПРЕСТУПЛЕНИИ</div>
      ${p(`${val(data, 'facts')}`)}
      <div class="request-title">ПРОШУ:</div>
      ${p(`${val(data, 'requests')}`)}
      <div class="legal-ref">Предупреждён(а) об ответственности за заведомо ложный донос по ст. 306 УК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-court-restore-term': {
    category: 'statement',
    title: 'Заявление о восстановлении процессуального срока (типовое)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'case_number', label: 'Номер дела/материала', type: 'text' },
      { name: 'term', label: 'Какой срок пропущен', type: 'text', placeholder: 'на подачу апелляционной жалобы' },
      { name: 'reasons', label: 'Причины пропуска', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `от ${val(data, 'applicant')}`, `по делу № ${val(data, 'case_number')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>о восстановлении процессуального срока</div>
      ${p(`Прошу восстановить пропущенный срок ${val(data, 'term')}. Причины пропуска: ${val(data, 'reasons')}.`)}
      <div class="legal-ref">На основании ст. 112 ГПК РФ (по ситуации),</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Восстановить процессуальный срок.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-fssp-application': {
    category: 'statement',
    title: 'Заявление приставу о совершении исполнительных действий',
    fields: [
      { name: 'to', label: 'ОСП/ФИО пристава', type: 'text' },
      { name: 'applicant', label: 'Взыскатель', type: 'text' },
      { name: 'ip_number', label: 'Номер ИП', type: 'text' },
      { name: 'requests', label: 'Какие действия просите', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `ИП № ${val(data, 'ip_number')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу совершить исполнительные действия: ${val(data, 'requests')}.`)}
      <div class="legal-ref">Основание: ФЗ «Об исполнительном производстве».</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-parental-leave': {
    category: 'statement',
    title: 'Заявление на отпуск по уходу за ребёнком',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'position', label: 'Должность', type: 'text', placeholder: 'менеджер' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text' },
      { name: 'start_date', label: 'С (дата)', type: 'date' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`, `должность: ${val(data, 'position')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу предоставить отпуск по уходу за ребёнком ${val(data, 'child')} с ${dateRu(data.start_date)}.`)}
      <div class="legal-ref">Основание: ст. 256 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-maternity-leave': {
    category: 'statement',
    title: 'Заявление на отпуск по беременности и родам',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'position', label: 'Должность', type: 'text', placeholder: 'менеджер' },
      { name: 'start_date', label: 'С (дата)', type: 'date' },
      { name: 'basis', label: 'Основание (лист нетрудоспособности №)', type: 'text' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`, `должность: ${val(data, 'position')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу предоставить отпуск по беременности и родам с ${dateRu(data.start_date)} на основании ${val(data, 'basis')}.`)}
      <div class="legal-ref">Основание: ст. 255 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-sick-pay': {
    category: 'statement',
    title: 'Заявление работодателю: назначить и выплатить пособие по временной нетрудоспособности',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'basis', label: 'Основание (ЭЛН/лист №)', type: 'text' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу назначить и выплатить пособие по временной нетрудоспособности на основании ${val(data, 'basis')}.`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-remote-work': {
    category: 'statement',
    title: 'Заявление о переводе на дистанционную работу (по соглашению)',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'reason', label: 'Причина (по желанию)', type: 'text' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу рассмотреть возможность перевода меня на дистанционную работу с ${dateRu(data.date)}. ${val(data, 'reason')}`)}
      <div class="legal-ref">Основание: гл. 49.1 ТК РФ (по соглашению сторон).</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-part-time': {
    category: 'statement',
    title: 'Заявление об установлении неполного рабочего времени',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'date', label: 'С (дата)', type: 'date' },
      { name: 'mode', label: 'Режим (часы/дни)', type: 'text', placeholder: 'например: 4 часа в день' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу установить мне неполное рабочее время с ${dateRu(data.date)}. Режим: ${val(data, 'mode')}.`)}
      <div class="legal-ref">Основание: ст. 93 ТК РФ (по ситуации).</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-issue-2ndfl': {
    category: 'statement',
    title: 'Заявление о выдаче справки о доходах (2-НДФЛ) / справки о заработке',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу выдать мне справку о доходах (2-НДФЛ) / справку о заработке за необходимый период.`)}
      <div class="legal-ref">Основание: ст. 62 ТК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-change-bank-details': {
    category: 'statement',
    title: 'Заявление о смене банковских реквизитов для выплаты зарплаты',
    fields: [
      { name: 'director', label: 'Кому (ФИО/должность)', type: 'text', placeholder: 'Директору ______' },
      { name: 'employee', label: 'От кого (ФИО)', type: 'text', placeholder: 'Иванов Иван Иванович' },
      { name: 'details', label: 'Новые реквизиты', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`${val(data, 'director')}`, `от ${val(data, 'employee')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу перечислять заработную плату по следующим реквизитам: ${val(data, 'details')}.`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'employee')} _________________`)}
    `
  },
  'statement-cancel-court-order': {
    category: 'statement',
    title: 'Заявление об отмене судебного приказа (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'debtor', label: 'Должник (ФИО)', type: 'text' },
      { name: 'order_date', label: 'Дата судебного приказа', type: 'date' },
      { name: 'case_number', label: 'Номер дела/приказа', type: 'text' },
      { name: 'reasons', label: 'Возражения (кратко: не согласен)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `от ${val(data, 'debtor')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>об отмене судебного приказа</div>
      ${p(`${dateRu(data.order_date)} вынесен судебный приказ по делу № ${val(data, 'case_number')}. Возражаю против его исполнения: ${val(data, 'reasons')}.`)}
      <div class="legal-ref">На основании ст. 129 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отменить судебный приказ.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'debtor')} _________________`)}
    `
  },
  'statement-issue-court-order': {
    category: 'statement',
    title: 'Заявление о выдаче судебного приказа (типовое, ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'creditor', label: 'Взыскатель (ФИО/ИП/ООО)', type: 'text' },
      { name: 'debtor', label: 'Должник', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'basis', label: 'Основание (договор/расписка)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `Взыскатель: ${val(data, 'creditor')}`, `Должник: ${val(data, 'debtor')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>о вынесении судебного приказа</div>
      ${p(`Прошу вынести судебный приказ о взыскании ${money(data, 'amount')} руб. Основание: ${val(data, 'basis')}.`)}
      <div class="legal-ref">На основании ст. 121–124 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Вынести судебный приказ о взыскании ${money(data, 'amount')} руб.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'creditor')} _________________`)}
    `
  },
  'statement-cancel-default-judgment': {
    category: 'statement',
    title: 'Заявление об отмене заочного решения (ГПК)',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'defendant', label: 'Ответчик (ФИО)', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'reasons', label: 'Причины неявки и доводы', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `от ${val(data, 'defendant')}`, `по делу № ${val(data, 'case_number')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>об отмене заочного решения</div>
      ${p(`Прошу отменить заочное решение по делу № ${val(data, 'case_number')} по следующим основаниям: ${val(data, 'reasons')}.`)}
      <div class="legal-ref">На основании ст. 237 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Отменить заочное решение и назначить рассмотрение дела по общим правилам.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'defendant')} _________________`)}
    `
  },
  'statement-installment-execution': {
    category: 'statement',
    title: 'Заявление об отсрочке/рассрочке исполнения решения суда',
    fields: [
      { name: 'court', label: 'Суд', type: 'text' },
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'request', label: 'Что просите (отсрочка/рассрочка, срок)', type: 'text' },
      { name: 'reasons', label: 'Основания (доход, здоровье и т.п.)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `от ${val(data, 'applicant')}`, `по делу № ${val(data, 'case_number')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ<br>об отсрочке/рассрочке исполнения</div>
      ${p(`Прошу предоставить ${val(data, 'request')}. Основания: ${val(data, 'reasons')}.`)}
      <div class="legal-ref">На основании ст. 203 ГПК РФ (по ситуации),</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`Предоставить ${val(data, 'request')}.`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-fns-tax-deduction': {
    category: 'statement',
    title: 'Заявление в ФНС: предоставление налогового вычета (общее)',
    fields: [
      { name: 'to', label: 'Инспекция ФНС', type: 'text' },
      { name: 'applicant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'inn', label: 'ИНН', type: 'text' },
      { name: 'type', label: 'Вид вычета', type: 'text', placeholder: 'имущественный/социальный/инвестиционный' },
      { name: 'year', label: 'Год', type: 'number' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `ИНН: ${val(data, 'inn')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу предоставить ${val(data, 'type')} налоговый вычет за ${money(data, 'year')} год (прилагаю документы).`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-police-check-kusp': {
    category: 'statement',
    title: 'Заявление в полицию: выдать талон-уведомление/сообщить номер КУСП (типовое)',
    fields: [
      { name: 'to', label: 'Куда (ОМВД)', type: 'text' },
      { name: 'applicant', label: 'Заявитель', type: 'text' },
      { name: 'date', label: 'Дата подачи заявления', type: 'date' },
      { name: 'facts', label: 'Что подавали', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`${dateRu(data.date)} мной подано заявление: ${val(data, 'facts')}.`)}
      ${p(`Прошу сообщить номер КУСП и выдать талон-уведомление (при наличии оснований).`)}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-bailiff-search': {
    category: 'statement',
    title: 'Заявление приставу: розыск имущества/счетов должника (типовое)',
    fields: [
      { name: 'to', label: 'ОСП/пристав', type: 'text' },
      { name: 'applicant', label: 'Взыскатель', type: 'text' },
      { name: 'ip_number', label: 'Номер ИП', type: 'text' },
      { name: 'requests', label: 'Какие меры просите', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'to')}`, `от ${val(data, 'applicant')}`, `ИП № ${val(data, 'ip_number')}`])}
      <div class="doc-title">ЗАЯВЛЕНИЕ</div>
      ${p(`Прошу принять меры: ${val(data, 'requests')}.`)}
      <div class="legal-ref">ФЗ «Об исполнительном производстве».</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'applicant')} _________________`)}
    `
  },
  'statement-appeal-gpk': {
    category: 'statement',
    title: 'Апелляционная жалоба (заготовка, ГПК)',
    fields: [
      { name: 'court', label: 'Суд апелляционной инстанции', type: 'text' },
      { name: 'from_court', label: 'Суд первой инстанции', type: 'text' },
      { name: 'appellant', label: 'Заявитель (ФИО)', type: 'text' },
      { name: 'case_number', label: 'Номер дела', type: 'text' },
      { name: 'decision_date', label: 'Дата решения', type: 'date' },
      { name: 'arguments', label: 'Доводы жалобы', type: 'textarea' },
      { name: 'requests', label: 'Просьбы (отменить/изменить)', type: 'textarea' }
    ],
    template: (data) => `
      ${rightBlock([`В ${val(data, 'court')}`, `через ${val(data, 'from_court')}`, `от ${val(data, 'appellant')}`, `по делу № ${val(data, 'case_number')}`])}
      <div class="doc-title">АПЕЛЛЯЦИОННАЯ ЖАЛОБА</div>
      ${p(`Обжалуется решение от ${dateRu(data.decision_date)} по делу № ${val(data, 'case_number')}.`)}
      ${p(`Доводы: ${val(data, 'arguments')}.`)}
      <div class="legal-ref">На основании гл. 39 ГПК РФ,</div>
      <div class="request-title">ПРОШУ:</div>
      ${reqBlock([`${val(data, 'requests')}`])}
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `${val(data, 'appellant')} _________________`)}
    `
  },

  // ==================== ДОГОВОРЫ (contract) ====================
  'contract-rent': {
    category: 'contract',
    title: 'Договор аренды квартиры',
    fields: [
      { name: 'landlord', label: 'Арендодатель (ФИО)', type: 'text', placeholder: 'Иванов И.И.' },
      { name: 'tenant', label: 'Арендатор (ФИО)', type: 'text', placeholder: 'Петров П.П.' },
      { name: 'address', label: 'Адрес квартиры', type: 'text', placeholder: 'г. Москва, ул. Ленина, д.1, кв.1' },
      { name: 'rent', label: 'Сумма аренды в месяц', type: 'number', placeholder: '30000' },
      { name: 'term', label: 'Срок договора (мес.)', type: 'number', placeholder: '12' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР АРЕНДЫ КВАРТИРЫ</div>
      <div class="text-block">г. Москва \t ${new Date().toLocaleDateString('ru-RU')}</div>
      <div class="text-block">Арендодатель: ${data.landlord || '_______________'}, Арендатор: ${data.tenant || '_______________'} заключили настоящий договор о нижеследующем:</div>
      <div class="text-block">1. Предмет договора: Арендодатель передает Арендатору квартиру по адресу: ${data.address || '_______________'}.</div>
      <div class="text-block">2. Арендная плата составляет ${data.rent || '_______'} руб. в месяц.</div>
      <div class="text-block">3. Срок договора: ${data.term || '_______'} месяцев.</div>
      <div class="text-block">Подписи сторон:</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.landlord}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.tenant}</td></tr></table>
      </div>
    `
  },
  'contract-services': {
    category: 'contract',
    title: 'Договор возмездного оказания услуг',
    fields: [
      { name: 'customer', label: 'Заказчик (ФИО/название)', type: 'text', placeholder: 'ООО "Ромашка"' },
      { name: 'executor', label: 'Исполнитель (ФИО/название)', type: 'text', placeholder: 'ИП Иванов И.И.' },
      { name: 'service', label: 'Описание услуг', type: 'textarea', placeholder: 'Консультационные услуги...' },
      { name: 'price', label: 'Стоимость услуг', type: 'number', placeholder: '50000' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ВОЗМЕЗДНОГО ОКАЗАНИЯ УСЛУГ</div>
      <div class="text-block">г. Москва \t ${new Date().toLocaleDateString('ru-RU')}</div>
      <div class="text-block">Заказчик: ${data.customer || '_______________'}, Исполнитель: ${data.executor || '_______________'} заключили настоящий договор о нижеследующем:</div>
      <div class="text-block">1. Исполнитель обязуется оказать услуги: ${data.service || '_______________'}.</div>
      <div class="text-block">2. Стоимость услуг составляет ${data.price || '_______'} руб.</div>
      <div class="text-block">3. Заказчик обязуется оплатить услуги в течение 5 дней после подписания акта.</div>
      <div class="text-block">Подписи сторон:</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.customer}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.executor}</td></tr></table>
      </div>
    `
  },
  'contract-loan': {
    category: 'contract',
    title: 'Договор займа между физическими лицами (типовой)',
    fields: [
      { name: 'lender', label: 'Займодавец (ФИО)', type: 'text' },
      { name: 'borrower', label: 'Заёмщик (ФИО)', type: 'text' },
      { name: 'amount', label: 'Сумма займа (руб.)', type: 'number' },
      { name: 'date', label: 'Дата передачи займа', type: 'date' },
      { name: 'return_date', label: 'Срок возврата', type: 'date' },
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ЗАЙМА</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`1. Стороны: Займодавец — ${val(data, 'lender')}, Заёмщик — ${val(data, 'borrower')}.`)}
      ${p(`2. Предмет: Займодавец передаёт Заёмщику денежные средства в сумме ${money(data, 'amount')} руб., а Заёмщик обязуется вернуть указанную сумму.`)}
      ${p(`3. Срок возврата: до ${dateRu(data.return_date)}.`)}
      ${p(`4. Прочие условия: по умолчанию займ считается беспроцентным (если иное не предусмотрено сторонами и законом).`)}
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'lender')}`, `___________ ${val(data, 'borrower')}`)}
    `
  },
  'contract-sale-car': {
    category: 'contract',
    title: 'Договор купли-продажи автомобиля (типовой)',
    fields: [
      { name: 'seller', label: 'Продавец (ФИО)', type: 'text' },
      { name: 'buyer', label: 'Покупатель (ФИО)', type: 'text' },
      { name: 'car', label: 'Авто (марка/модель)', type: 'text' },
      { name: 'vin', label: 'VIN', type: 'text' },
      { name: 'year', label: 'Год выпуска', type: 'number' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР КУПЛИ-ПРОДАЖИ АВТОМОБИЛЯ</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Продавец: ${val(data, 'seller')}. Покупатель: ${val(data, 'buyer')}.`)}
      ${p(`2. Предмет: автомобиль ${val(data, 'car')}, VIN ${val(data, 'vin')}, ${money(data, 'year')} г.в.`)}
      ${p(`3. Цена: ${money(data, 'price')} руб. Порядок расчётов: по соглашению сторон.`)}
      ${p(`4. Передача автомобиля и документов осуществляется при подписании договора/акта (по соглашению).`)}
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'seller')}`, `___________ ${val(data, 'buyer')}`)}
    `
  },
  'contract-supply': {
    category: 'contract',
    title: 'Договор поставки (базовый)',
    fields: [
      { name: 'supplier', label: 'Поставщик', type: 'text' },
      { name: 'buyer', label: 'Покупатель', type: 'text' },
      { name: 'goods', label: 'Товар (наименование)', type: 'text' },
      { name: 'price', label: 'Цена/стоимость (руб.)', type: 'number' },
      { name: 'terms', label: 'Сроки/условия поставки (кратко)', type: 'textarea' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ПОСТАВКИ</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Стороны: Поставщик — ${val(data, 'supplier')}, Покупатель — ${val(data, 'buyer')}.`)}
      ${p(`2. Предмет: поставка товара — ${val(data, 'goods')}.`)}
      ${p(`3. Стоимость: ${money(data, 'price')} руб. (или по спецификации).`)}
      ${p(`4. Условия поставки: ${val(data, 'terms')}.`)}
      <div class="legal-ref">Основание: гл. 30 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'supplier')}`, `___________ ${val(data, 'buyer')}`)}
    `
  },
  'contract-contracting': {
    category: 'contract',
    title: 'Договор подряда (базовый)',
    fields: [
      { name: 'contractor', label: 'Подрядчик', type: 'text' },
      { name: 'customer', label: 'Заказчик', type: 'text' },
      { name: 'work', label: 'Работы (описание)', type: 'textarea' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'deadline', label: 'Срок выполнения (кратко)', type: 'text' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ПОДРЯДА</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Подрядчик: ${val(data, 'contractor')}. Заказчик: ${val(data, 'customer')}.`)}
      ${p(`2. Предмет работ: ${val(data, 'work')}.`)}
      ${p(`3. Цена: ${money(data, 'price')} руб.`)}
      ${p(`4. Срок выполнения: ${val(data, 'deadline')}.`)}
      <div class="legal-ref">Основание: гл. 37 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'contractor')}`, `___________ ${val(data, 'customer')}`)}
    `
  },
  'contract-lease-nonres': {
    category: 'contract',
    title: 'Договор аренды нежилого помещения (базовый)',
    fields: [
      { name: 'landlord', label: 'Арендодатель', type: 'text' },
      { name: 'tenant', label: 'Арендатор', type: 'text' },
      { name: 'address', label: 'Адрес помещения', type: 'text' },
      { name: 'rent', label: 'Арендная плата (руб./мес.)', type: 'number' },
      { name: 'term', label: 'Срок (мес.)', type: 'number' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР АРЕНДЫ НЕЖИЛОГО ПОМЕЩЕНИЯ</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Объект аренды: ${val(data, 'address')}.`)}
      ${p(`2. Арендная плата: ${money(data, 'rent')} руб./мес.`)}
      ${p(`3. Срок: ${money(data, 'term')} мес.`)}
      <div class="legal-ref">Основание: гл. 34 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'landlord')}`, `___________ ${val(data, 'tenant')}`)}
    `
  },
  'contract-agency': {
    category: 'contract',
    title: 'Агентский договор (базовый)',
    fields: [
      { name: 'principal', label: 'Принципал', type: 'text' },
      { name: 'agent', label: 'Агент', type: 'text' },
      { name: 'actions', label: 'Поручаемые действия', type: 'textarea' },
      { name: 'fee', label: 'Вознаграждение (руб.)', type: 'number' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">АГЕНТСКИЙ ДОГОВОР</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Принципал: ${val(data, 'principal')}. Агент: ${val(data, 'agent')}.`)}
      ${p(`2. Агент обязуется совершать действия: ${val(data, 'actions')}.`)}
      ${p(`3. Вознаграждение агента: ${money(data, 'fee')} руб. (или по расчёту).`)}
      <div class="legal-ref">Основание: гл. 52 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'principal')}`, `___________ ${val(data, 'agent')}`)}
    `
  },
  'contract-pledge': {
    category: 'contract',
    title: 'Договор залога (базовый)',
    fields: [
      { name: 'pledger', label: 'Залогодатель', type: 'text' },
      { name: 'pledgee', label: 'Залогодержатель', type: 'text' },
      { name: 'subject', label: 'Предмет залога', type: 'textarea' },
      { name: 'secured_amount', label: 'Обеспечиваемое обязательство (сумма, руб.)', type: 'number' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ЗАЛОГА</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Залогодатель: ${val(data, 'pledger')}. Залогодержатель: ${val(data, 'pledgee')}.`)}
      ${p(`2. Предмет залога: ${val(data, 'subject')}.`)}
      ${p(`3. Залог обеспечивает обязательство на сумму ${money(data, 'secured_amount')} руб.`)}
      <div class="legal-ref">Основание: гл. 23 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'pledger')}`, `___________ ${val(data, 'pledgee')}`)}
    `
  },
  'contract-deposit': {
    category: 'contract',
    title: 'Договор задатка (типовой)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'main_deal', label: 'Основная сделка (что планируется)', type: 'textarea' },
      { name: 'deposit', label: 'Задаток (руб.)', type: 'number' },
      { name: 'city', label: 'Город', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ЗАДАТКЕ</div>
      ${p(`${val(data, 'city')} \t ${new Date().toLocaleDateString('ru-RU')}`)}
      ${p(`1. Сторона 1: ${val(data, 'party1')}. Сторона 2: ${val(data, 'party2')}.`)}
      ${p(`2. В обеспечение обязательств по основной сделке: ${val(data, 'main_deal')}, сторона 1 передаёт задаток ${money(data, 'deposit')} руб.`)}
      <div class="legal-ref">Основание: ст. 380–381 ГК РФ.</div>
      <div class="text-block">Подписи сторон:</div>
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'contract-preliminary-sale': {
    category: 'contract',
    title: 'Предварительный договор купли-продажи (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'seller', label: 'Продавец', type: 'text' },
      { name: 'buyer', label: 'Покупатель', type: 'text' },
      { name: 'subject', label: 'Объект (описание)', type: 'textarea' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'main_term', label: 'Срок заключения основного договора', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ПРЕДВАРИТЕЛЬНЫЙ ДОГОВОР</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Продавец: ${val(data, 'seller')}, Покупатель: ${val(data, 'buyer')}.`)}
      ${p(`Стороны обязуются заключить основной договор купли-продажи в срок: ${val(data, 'main_term')}.`)}
      ${p(`Объект: ${val(data, 'subject')}. Цена: ${money(data, 'price')} руб.`)}
      <div class="legal-ref">Основание: ст. 429 ГК РФ.</div>
      ${signBlock(`Дата: ${new Date().toLocaleDateString('ru-RU')}`, `Подписи: ___________ / ___________`)}
    `
  },
  'contract-sale-apartment-simple': {
    category: 'contract',
    title: 'Договор купли-продажи квартиры (упрощённый шаблон)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'seller', label: 'Продавец (ФИО)', type: 'text' },
      { name: 'buyer', label: 'Покупатель (ФИО)', type: 'text' },
      { name: 'address', label: 'Адрес квартиры', type: 'text' },
      { name: 'cad', label: 'Кадастровый номер', type: 'text' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР КУПЛИ-ПРОДАЖИ КВАРТИРЫ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Продавец: ${val(data, 'seller')}. Покупатель: ${val(data, 'buyer')}.`)}
      ${p(`Объект: квартира по адресу ${val(data, 'address')}, кадастровый № ${val(data, 'cad')}.`)}
      ${p(`Цена: ${money(data, 'price')} руб. Порядок расчётов и передачи — по соглашению сторон.`)}
      <div class="legal-ref">ГК РФ, ст. 549–558 (по ситуации).</div>
      ${signBlock(`Подписи сторон: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-gift-money': {
    category: 'contract',
    title: 'Договор дарения денежных средств (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'donor', label: 'Даритель', type: 'text' },
      { name: 'donee', label: 'Одаряемый', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ДАРЕНИЯ ДЕНЕЖНЫХ СРЕДСТВ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Даритель: ${val(data, 'donor')}. Одаряемый: ${val(data, 'donee')}.`)}
      ${p(`Даритель безвозмездно передаёт Одаряемому денежные средства в размере ${money(data, 'amount')} руб.`)}
      <div class="legal-ref">Основание: гл. 32 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-lease-car': {
    category: 'contract',
    title: 'Договор аренды автомобиля (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'landlord', label: 'Арендодатель', type: 'text' },
      { name: 'tenant', label: 'Арендатор', type: 'text' },
      { name: 'car', label: 'Авто (марка/модель, VIN)', type: 'textarea' },
      { name: 'rent', label: 'Плата (руб.)', type: 'number' },
      { name: 'term', label: 'Срок (текст)', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР АРЕНДЫ АВТОМОБИЛЯ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Арендодатель: ${val(data, 'landlord')}. Арендатор: ${val(data, 'tenant')}.`)}
      ${p(`Предмет: ${val(data, 'car')}.`)}
      ${p(`Плата: ${money(data, 'rent')} руб. Срок: ${val(data, 'term')}.`)}
      <div class="legal-ref">Основание: гл. 34 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-borrowing': {
    category: 'contract',
    title: 'Договор безвозмездного пользования (ссуды) (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'lender', label: 'Ссудодатель', type: 'text' },
      { name: 'borrower', label: 'Ссудополучатель', type: 'text' },
      { name: 'thing', label: 'Имущество (описание)', type: 'textarea' },
      { name: 'term', label: 'Срок', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР БЕЗВОЗМЕЗДНОГО ПОЛЬЗОВАНИЯ (ССУДЫ)</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Ссудодатель: ${val(data, 'lender')}. Ссудополучатель: ${val(data, 'borrower')}.`)}
      ${p(`Имущество: ${val(data, 'thing')}. Срок: ${val(data, 'term')}.`)}
      <div class="legal-ref">Основание: гл. 36 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-surety': {
    category: 'contract',
    title: 'Договор поручительства (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'creditor', label: 'Кредитор', type: 'text' },
      { name: 'debtor', label: 'Должник', type: 'text' },
      { name: 'surety', label: 'Поручитель', type: 'text' },
      { name: 'amount', label: 'Обязательство (сумма, руб.)', type: 'number' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ПОРУЧИТЕЛЬСТВА</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Кредитор: ${val(data, 'creditor')}. Должник: ${val(data, 'debtor')}. Поручитель: ${val(data, 'surety')}.`)}
      ${p(`Поручитель отвечает за исполнение обязательства должника на сумму ${money(data, 'amount')} руб. (условия — по соглашению сторон).`)}
      <div class="legal-ref">Основание: ст. 361–367 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-commission': {
    category: 'contract',
    title: 'Договор комиссии (базовый)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'committent', label: 'Комитент', type: 'text' },
      { name: 'commissioner', label: 'Комиссионер', type: 'text' },
      { name: 'task', label: 'Поручение (что сделать)', type: 'textarea' },
      { name: 'fee', label: 'Вознаграждение (руб.)', type: 'number' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР КОМИССИИ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Комитент: ${val(data, 'committent')}. Комиссионер: ${val(data, 'commissioner')}.`)}
      ${p(`Поручение: ${val(data, 'task')}. Вознаграждение: ${money(data, 'fee')} руб.`)}
      <div class="legal-ref">Основание: гл. 51 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-storage': {
    category: 'contract',
    title: 'Договор хранения (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'keeper', label: 'Хранитель', type: 'text' },
      { name: 'depositor', label: 'Поклажедатель', type: 'text' },
      { name: 'item', label: 'Имущество', type: 'textarea' },
      { name: 'term', label: 'Срок хранения', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ХРАНЕНИЯ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Хранитель: ${val(data, 'keeper')}. Поклажедатель: ${val(data, 'depositor')}.`)}
      ${p(`Имущество: ${val(data, 'item')}. Срок хранения: ${val(data, 'term')}.`)}
      <div class="legal-ref">Основание: гл. 47 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-transportation': {
    category: 'contract',
    title: 'Договор перевозки груза (упрощённый)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'carrier', label: 'Перевозчик', type: 'text' },
      { name: 'customer', label: 'Заказчик', type: 'text' },
      { name: 'cargo', label: 'Груз (описание)', type: 'textarea' },
      { name: 'route', label: 'Маршрут', type: 'text' },
      { name: 'price', label: 'Стоимость (руб.)', type: 'number' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР ПЕРЕВОЗКИ ГРУЗА</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Перевозчик: ${val(data, 'carrier')}. Заказчик: ${val(data, 'customer')}.`)}
      ${p(`Груз: ${val(data, 'cargo')}. Маршрут: ${val(data, 'route')}. Стоимость: ${money(data, 'price')} руб.`)}
      <div class="legal-ref">Основание: гл. 40 ГК РФ (в части общих положений) и спец. нормы (по виду транспорта).</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-author-order': {
    category: 'contract',
    title: 'Договор авторского заказа (упрощённый)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'customer', label: 'Заказчик', type: 'text' },
      { name: 'author', label: 'Автор', type: 'text' },
      { name: 'work', label: 'Произведение/результат', type: 'textarea' },
      { name: 'fee', label: 'Вознаграждение (руб.)', type: 'number' },
      { name: 'deadline', label: 'Срок', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР АВТОРСКОГО ЗАКАЗА</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Заказчик: ${val(data, 'customer')}. Автор: ${val(data, 'author')}.`)}
      ${p(`Результат: ${val(data, 'work')}. Срок: ${val(data, 'deadline')}. Вознаграждение: ${money(data, 'fee')} руб.`)}
      <div class="legal-ref">Основание: ГК РФ (часть IV), ст. 1288 (по ситуации).</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-household-contracting': {
    category: 'contract',
    title: 'Договор бытового подряда (для физлица) (упрощённый)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'contractor', label: 'Подрядчик (ИП/ООО)', type: 'text' },
      { name: 'customer', label: 'Заказчик (ФИО)', type: 'text' },
      { name: 'work', label: 'Работы', type: 'textarea' },
      { name: 'price', label: 'Цена (руб.)', type: 'number' },
      { name: 'deadline', label: 'Срок', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР БЫТОВОГО ПОДРЯДА</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Подрядчик: ${val(data, 'contractor')}. Заказчик: ${val(data, 'customer')}.`)}
      ${p(`Работы: ${val(data, 'work')}. Цена: ${money(data, 'price')} руб. Срок: ${val(data, 'deadline')}.`)}
      <div class="legal-ref">Основание: ст. 730–739 ГК РФ; Закон РФ «О защите прав потребителей» (по ситуации).</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-rent-room': {
    category: 'contract',
    title: 'Договор найма комнаты (типовой)',
    fields: [
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' },
      { name: 'landlord', label: 'Наймодатель', type: 'text' },
      { name: 'tenant', label: 'Наниматель', type: 'text' },
      { name: 'address', label: 'Адрес', type: 'text' },
      { name: 'rent', label: 'Плата (руб./мес.)', type: 'number' },
      { name: 'term', label: 'Срок', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР НАЙМА КОМНАТЫ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Наймодатель: ${val(data, 'landlord')}. Наниматель: ${val(data, 'tenant')}.`)}
      ${p(`Объект: комната по адресу ${val(data, 'address')}.`)}
      ${p(`Плата: ${money(data, 'rent')} руб./мес. Срок: ${val(data, 'term')}.`)}
      <div class="legal-ref">Основание: гл. 35 ГК РФ (найм жилого помещения).</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'contract-acceptance-act': {
    category: 'contract',
    title: 'Акт приёма-передачи (универсальный к договору) (шаблон)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'contract', label: 'Договор (№/дата)', type: 'text' },
      { name: 'subject', label: 'Что передаётся', type: 'textarea' },
      { name: 'city', label: 'Город', type: 'text', placeholder: 'г. Москва' },
      { name: 'date', label: 'Дата', type: 'date' }
    ],
    template: (data) => `
      <div class="doc-title">АКТ ПРИЁМА-ПЕРЕДАЧИ</div>
      ${p(`${val(data, 'city')} \t ${dateRu(data.date)}`)}
      ${p(`Мы, ${val(data, 'party1')} и ${val(data, 'party2')}, составили настоящий акт к договору ${val(data, 'contract')}.`)}
      ${p(`Передано/принято: ${val(data, 'subject')}.`)}
      ${p(`Стороны претензий не имеют (если иное не указано).`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },

  // ==================== СОГЛАШЕНИЯ (agreement) ====================
  'agreement-confidentiality': {
    category: 'agreement',
    title: 'Соглашение о конфиденциальности',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text', placeholder: 'ООО "А"' },
      { name: 'party2', label: 'Сторона 2', type: 'text', placeholder: 'ООО "Б"' },
      { name: 'info', label: 'Предмет конфиденциальности', type: 'textarea', placeholder: 'Коммерческая тайна...' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О КОНФИДЕНЦИАЛЬНОСТИ</div>
      <div class="text-block">г. Москва \t ${new Date().toLocaleDateString('ru-RU')}</div>
      <div class="text-block">${data.party1 || '_______________'} и ${data.party2 || '_______________'} (далее – Стороны) заключили настоящее соглашение о нижеследующем:</div>
      <div class="text-block">1. Стороны обязуются не разглашать конфиденциальную информацию: ${data.info || '_______________'}.</div>
      <div class="text-block">2. Настоящее соглашение действует в течение 3 лет.</div>
      <div class="text-block">Подписи сторон:</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.party1}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.party2}</td></tr></table>
      </div>
    `
  },
  'agreement-division': {
    category: 'agreement',
    title: 'Соглашение о разделе имущества',
    fields: [
      { name: 'spouse1', label: 'Супруг 1', type: 'text', placeholder: 'Иванов И.И.' },
      { name: 'spouse2', label: 'Супруг 2', type: 'text', placeholder: 'Иванова М.С.' },
      { name: 'property', label: 'Имущество (описание и доли)', type: 'textarea', placeholder: 'Квартира – 1/2, автомобиль – 1/2' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О РАЗДЕЛЕ ИМУЩЕСТВА</div>
      <div class="text-block">г. Москва \t ${new Date().toLocaleDateString('ru-RU')}</div>
      <div class="text-block">Мы, ${data.spouse1 || '_______________'} и ${data.spouse2 || '_______________'}, заключили настоящее соглашение о разделе совместно нажитого имущества:</div>
      <div class="text-block">${data.property || '_______________'}</div>
      <div class="text-block">Подписи сторон:</div>
      <div style="display: flex; justify-content: space-between; width: 100%; margin-top: 2em;">
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.spouse1}</td></tr></table>
        <table style="border-collapse: collapse; width: auto;"><tr><td style="border: none; padding: 0;">___________ ${data.spouse2}</td></tr></table>
      </div>
    `
  },
  'agreement-settlement': {
    category: 'agreement',
    title: 'Мировое соглашение (заготовка)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'case_number', label: 'Номер дела (если есть)', type: 'text' },
      { name: 'terms', label: 'Условия урегулирования', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">МИРОВОЕ СОГЛАШЕНИЕ</div>
      ${p(`Сторона 1: ${val(data, 'party1')}. Сторона 2: ${val(data, 'party2')}.`)}
      ${p(`${data.case_number ? `По делу № ${val(data, 'case_number')}.` : ''}`)}
      ${p(`Стороны договорились о следующем: ${val(data, 'terms')}.`)}
      ${p(`Стороны подтверждают, что условия понятны, права не нарушаются, соглашение заключено добровольно.`)}
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'agreement-alimony': {
    category: 'agreement',
    title: 'Соглашение об уплате алиментов (заготовка)',
    fields: [
      { name: 'payer', label: 'Плательщик', type: 'text' },
      { name: 'receiver', label: 'Получатель', type: 'text' },
      { name: 'child', label: 'Ребёнок (ФИО, дата рождения)', type: 'text' },
      { name: 'amount', label: 'Размер (руб.) или порядок', type: 'text', placeholder: '___ руб. ежемесячно' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ ОБ УПЛАТЕ АЛИМЕНТОВ</div>
      ${p(`Плательщик: ${val(data, 'payer')}. Получатель: ${val(data, 'receiver')}.`)}
      ${p(`Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Порядок и размер алиментов: ${val(data, 'amount')}.`)}
      <div class="legal-ref">Важно: в общем случае соглашение об уплате алиментов подлежит нотариальному удостоверению (СК РФ).</div>
      ${signBlock(`___________ ${val(data, 'payer')}`, `___________ ${val(data, 'receiver')}`)}
    `
  },
  'agreement-termination': {
    category: 'agreement',
    title: 'Соглашение о расторжении договора (типовое)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'contract', label: 'Договор (номер/дата)', type: 'text' },
      { name: 'date', label: 'Дата расторжения', type: 'date' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О РАСТОРЖЕНИИ ДОГОВОРА</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Договор: ${val(data, 'contract')}.`)}
      ${p(`Стороны договорились расторгнуть договор с ${dateRu(data.date)}. Претензий друг к другу (указать: есть/нет).`)}
      <div class="legal-ref">Основание: ст. 450 ГК РФ.</div>
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'agreement-assignment': {
    category: 'agreement',
    title: 'Договор уступки права требования (цессия) (базовый)',
    fields: [
      { name: 'cedent', label: 'Цедент', type: 'text' },
      { name: 'cessionary', label: 'Цессионарий', type: 'text' },
      { name: 'debtor', label: 'Должник', type: 'text' },
      { name: 'claim', label: 'Требование (описание)', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">ДОГОВОР УСТУПКИ ПРАВА ТРЕБОВАНИЯ</div>
      ${p(`Цедент: ${val(data, 'cedent')}. Цессионарий: ${val(data, 'cessionary')}. Должник: ${val(data, 'debtor')}.`)}
      ${p(`Уступаемое требование: ${val(data, 'claim')}.`)}
      <div class="legal-ref">Основание: ст. 382–390 ГК РФ.</div>
      ${signBlock(`___________ ${val(data, 'cedent')}`, `___________ ${val(data, 'cessionary')}`)}
    `
  },
  'agreement-installment': {
    category: 'agreement',
    title: 'Соглашение о рассрочке/графике погашения долга',
    fields: [
      { name: 'creditor', label: 'Кредитор', type: 'text' },
      { name: 'debtor', label: 'Должник', type: 'text' },
      { name: 'debt', label: 'Сумма долга (руб.)', type: 'number' },
      { name: 'schedule', label: 'График (текстом)', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О РАССРОЧКЕ</div>
      ${p(`Кредитор: ${val(data, 'creditor')}. Должник: ${val(data, 'debtor')}.`)}
      ${p(`Сумма долга: ${money(data, 'debt')} руб.`)}
      ${p(`График погашения: ${val(data, 'schedule')}.`)}
      <div class="legal-ref">Основание: ст. 309 ГК РФ.</div>
      ${signBlock(`___________ ${val(data, 'creditor')}`, `___________ ${val(data, 'debtor')}`)}
    `
  },
  'agreement-change': {
    category: 'agreement',
    title: 'Дополнительное соглашение к договору (изменение условий)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'contract', label: 'Договор (номер/дата)', type: 'text' },
      { name: 'changes', label: 'Какие условия меняются', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">ДОПОЛНИТЕЛЬНОЕ СОГЛАШЕНИЕ</div>
      ${p(`к договору ${val(data, 'contract')}`)}
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Стороны договорились изменить условия договора следующим образом: ${val(data, 'changes')}.`)}
      <div class="legal-ref">Основание: ст. 450, 452 ГК РФ.</div>
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'agreement-pretension-settlement': {
    category: 'agreement',
    title: 'Соглашение об урегулировании претензии (внесудебно)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'subject', label: 'Суть спора/претензии', type: 'textarea' },
      { name: 'terms', label: 'Условия урегулирования', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ ОБ УРЕГУЛИРОВАНИИ СПОРА</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Предмет: ${val(data, 'subject')}.`)}
      ${p(`Условия урегулирования: ${val(data, 'terms')}.`)}
      ${p(`После исполнения условий стороны не имеют взаимных претензий (если иное не указано).`)}
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'agreement-mediation': {
    category: 'agreement',
    title: 'Соглашение о проведении медиации (заготовка)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'mediator', label: 'Медиатор/организация', type: 'text' },
      { name: 'subject', label: 'Предмет разногласий', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ПРОВЕДЕНИИ МЕДИАЦИИ</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}. Медиатор: ${val(data, 'mediator')}.`)}
      ${p(`Предмет разногласий: ${val(data, 'subject')}.`)}
      <div class="legal-ref">Основание: ФЗ № 193-ФЗ «Об альтернативной процедуре урегулирования споров с участием посредника (процедуре медиации)».</div>
      ${signBlock(`___________ ${val(data, 'party1')}`, `___________ ${val(data, 'party2')}`)}
    `
  },
  'agreement-debt-acknowledgement': {
    category: 'agreement',
    title: 'Расписка/соглашение о признании долга (типовое)',
    fields: [
      { name: 'debtor', label: 'Должник (ФИО/ИП)', type: 'text' },
      { name: 'creditor', label: 'Кредитор (ФИО/ИП)', type: 'text' },
      { name: 'amount', label: 'Сумма долга (руб.)', type: 'number' },
      { name: 'basis', label: 'Основание долга', type: 'textarea' },
      { name: 'return_date', label: 'Срок возврата', type: 'date' }
    ],
    template: (data) => `
      <div class="doc-title">РАСПИСКА / ПРИЗНАНИЕ ДОЛГА</div>
      ${p(`Я, ${val(data, 'debtor')}, признаю задолженность перед ${val(data, 'creditor')} в сумме ${money(data, 'amount')} руб.`)}
      ${p(`Основание: ${val(data, 'basis')}.`)}
      ${p(`Обязуюсь вернуть сумму долга до ${dateRu(data.return_date)}.`)}
      <div class="legal-ref">ГК РФ (общие положения об обязательствах).</div>
      ${signBlock(`${val(data, 'debtor')} _________________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-setoff': {
    category: 'agreement',
    title: 'Соглашение о зачёте взаимных требований (типовое)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'claims', label: 'Какие требования зачитываются', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ЗАЧЁТЕ</div>
      ${p(`Сторона 1: ${val(data, 'party1')}, Сторона 2: ${val(data, 'party2')}.`)}
      ${p(`Стороны договорились произвести зачёт следующих требований: ${val(data, 'claims')}.`)}
      <div class="legal-ref">Основание: ст. 410–412 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-novation': {
    category: 'agreement',
    title: 'Соглашение о новации обязательства (базовое)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'old', label: 'Старое обязательство', type: 'textarea' },
      { name: 'new', label: 'Новое обязательство', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О НОВАЦИИ</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Стороны прекращают обязательство: ${val(data, 'old')} и заменяют его новым: ${val(data, 'new')}.`)}
      <div class="legal-ref">Основание: ст. 414 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-child-residence': {
    category: 'agreement',
    title: 'Соглашение родителей о месте жительства ребёнка (заготовка)',
    fields: [
      { name: 'parent1', label: 'Родитель 1', type: 'text' },
      { name: 'parent2', label: 'Родитель 2', type: 'text' },
      { name: 'child', label: 'Ребёнок', type: 'text' },
      { name: 'address', label: 'Адрес проживания ребёнка', type: 'text' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О МЕСТЕ ЖИТЕЛЬСТВА РЕБЁНКА</div>
      ${p(`Родители: ${val(data, 'parent1')} и ${val(data, 'parent2')}.`)}
      ${p(`Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Стороны определили место жительства ребёнка по адресу: ${val(data, 'address')}.`)}
      <div class="legal-ref">Основание: ст. 65 СК РФ (по ситуации).</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-child-communication': {
    category: 'agreement',
    title: 'Соглашение о порядке общения с ребёнком (заготовка)',
    fields: [
      { name: 'parent1', label: 'Родитель 1', type: 'text' },
      { name: 'parent2', label: 'Родитель 2', type: 'text' },
      { name: 'child', label: 'Ребёнок', type: 'text' },
      { name: 'schedule', label: 'График общения', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ПОРЯДКЕ ОБЩЕНИЯ С РЕБЁНКОМ</div>
      ${p(`Родители: ${val(data, 'parent1')} и ${val(data, 'parent2')}. Ребёнок: ${val(data, 'child')}.`)}
      ${p(`Порядок общения: ${val(data, 'schedule')}.`)}
      <div class="legal-ref">Основание: ст. 66 СК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-lease-termination-specific': {
    category: 'agreement',
    title: 'Соглашение о расторжении договора аренды (типовое)',
    fields: [
      { name: 'landlord', label: 'Арендодатель', type: 'text' },
      { name: 'tenant', label: 'Арендатор', type: 'text' },
      { name: 'contract', label: 'Договор аренды (№/дата)', type: 'text' },
      { name: 'date', label: 'Дата расторжения', type: 'date' },
      { name: 'settlement', label: 'Расчёты/залог/претензии', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О РАСТОРЖЕНИИ ДОГОВОРА АРЕНДЫ</div>
      ${p(`Стороны: ${val(data, 'landlord')} и ${val(data, 'tenant')}.`)}
      ${p(`Договор: ${val(data, 'contract')}.`)}
      ${p(`Расторжение с ${dateRu(data.date)}.`)}
      ${p(`Порядок расчётов/претензий: ${val(data, 'settlement')}.`)}
      <div class="legal-ref">Основание: ст. 450 ГК РФ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-reconciliation-act': {
    category: 'agreement',
    title: 'Акт сверки взаиморасчётов (шаблон)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'period', label: 'Период', type: 'text' },
      { name: 'result', label: 'Итог/сальдо', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">АКТ СВЕРКИ ВЗАИМОРАСЧЁТОВ</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Период: ${val(data, 'period')}.`)}
      ${p(`Итог: ${val(data, 'result')}.`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-waiver-claims': {
    category: 'agreement',
    title: 'Соглашение об отсутствии взаимных претензий (типовое)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'basis', label: 'Основание (договор/событие)', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ ОБ ОТСУТСТВИИ ВЗАИМНЫХ ПРЕТЕНЗИЙ</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Основание: ${val(data, 'basis')}.`)}
      ${p(`Стороны подтверждают отсутствие взаимных претензий (если иное не указано).`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-advance-return': {
    category: 'agreement',
    title: 'Соглашение о возврате аванса/предоплаты (типовое)',
    fields: [
      { name: 'party1', label: 'Сторона, получившая аванс', type: 'text' },
      { name: 'party2', label: 'Сторона, внесшая аванс', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'deadline', label: 'Срок возврата', type: 'date' },
      { name: 'basis', label: 'Основание', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ВОЗВРАТЕ АВАНСА</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Основание: ${val(data, 'basis')}.`)}
      ${p(`Сумма к возврату: ${money(data, 'amount')} руб. Срок: до ${dateRu(data.deadline)}.`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-consent-personal-data': {
    category: 'agreement',
    title: 'Согласие на обработку персональных данных (шаблон)',
    fields: [
      { name: 'subject', label: 'Субъект ПДн (ФИО)', type: 'text' },
      { name: 'operator', label: 'Оператор (организация)', type: 'text' },
      { name: 'purpose', label: 'Цель обработки', type: 'textarea' },
      { name: 'term', label: 'Срок согласия', type: 'text', placeholder: 'до достижения цели/___ лет' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАСИЕ НА ОБРАБОТКУ ПЕРСОНАЛЬНЫХ ДАННЫХ</div>
      ${p(`Я, ${val(data, 'subject')}, даю согласие ${val(data, 'operator')} на обработку моих персональных данных в целях: ${val(data, 'purpose')}.`)}
      ${p(`Срок согласия: ${val(data, 'term')}. Отзыв — в порядке, предусмотренном законом.`)}
      <div class="legal-ref">Основание: ФЗ № 152-ФЗ «О персональных данных».</div>
      ${signBlock(`${val(data, 'subject')} _________________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-data-processing': {
    category: 'agreement',
    title: 'Соглашение об обработке ПДн (клиент—исполнитель) (упрощённое)',
    fields: [
      { name: 'party1', label: 'Сторона 1', type: 'text' },
      { name: 'party2', label: 'Сторона 2', type: 'text' },
      { name: 'data', label: 'Какие данные', type: 'textarea' },
      { name: 'purpose', label: 'Цель', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ ОБ ОБРАБОТКЕ ПДн</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Перечень данных: ${val(data, 'data')}.`)}
      ${p(`Цель обработки: ${val(data, 'purpose')}.`)}
      <div class="legal-ref">ФЗ № 152-ФЗ.</div>
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-penalty-reduction-request': {
    category: 'agreement',
    title: 'Соглашение о снижении неустойки/штрафа (внесудебно) (шаблон)',
    fields: [
      { name: 'creditor', label: 'Кредитор', type: 'text' },
      { name: 'debtor', label: 'Должник', type: 'text' },
      { name: 'old', label: 'Исходная неустойка (руб.)', type: 'number' },
      { name: 'new', label: 'Согласованная неустойка (руб.)', type: 'number' },
      { name: 'basis', label: 'Основание/условия', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О РЕСТРУКТУРИЗАЦИИ/СНИЖЕНИИ НЕУСТОЙКИ</div>
      ${p(`Кредитор: ${val(data, 'creditor')}. Должник: ${val(data, 'debtor')}.`)}
      ${p(`Стороны согласовали снижение неустойки с ${money(data, 'old')} руб. до ${money(data, 'new')} руб.`)}
      ${p(`Условия: ${val(data, 'basis')}.`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  },
  'agreement-payment-schedule': {
    category: 'agreement',
    title: 'Соглашение о графике платежей (универсальное)',
    fields: [
      { name: 'party1', label: 'Кредитор/исполнитель', type: 'text' },
      { name: 'party2', label: 'Плательщик/должник', type: 'text' },
      { name: 'amount', label: 'Сумма (руб.)', type: 'number' },
      { name: 'schedule', label: 'График платежей', type: 'textarea' }
    ],
    template: (data) => `
      <div class="doc-title">СОГЛАШЕНИЕ О ГРАФИКЕ ПЛАТЕЖЕЙ</div>
      ${p(`Стороны: ${val(data, 'party1')} и ${val(data, 'party2')}.`)}
      ${p(`Общая сумма: ${money(data, 'amount')} руб.`)}
      ${p(`График: ${val(data, 'schedule')}.`)}
      ${signBlock(`Подписи: ___________ / ___________`, `Дата: ${new Date().toLocaleDateString('ru-RU')}`)}
    `
  }
};
